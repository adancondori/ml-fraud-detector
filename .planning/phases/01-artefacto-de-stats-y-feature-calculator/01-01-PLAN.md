---
phase: 01-artefacto-de-stats-y-feature-calculator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fraud_detector/stats/__init__.py
  - src/fraud_detector/stats/tz_mapping.py
  - src/fraud_detector/stats/builder.py
  - src/fraud_detector/stats/validator.py
  - scripts/build_facility_stats.py
  - output/models/facility_stats_v1.json
  - tests/test_facility_stats_builder.py
autonomous: true

must_haves:
  truths:
    - "Existe output/models/facility_stats_v1.json versionado con median/iqr/iqr_guarded/mean/n/iana_tz/fallback_level por facility"
    - "Toda facility (1876) tiene entrada iana_tz; ninguna queda sin zona (13 zonas Rails faltantes del prototipo mapeadas)"
    - "Toda facility con n<30 recibe fallback_level='currency' o 'global'; ninguna facility queda sin stats de magnitud usables"
    - "El validator confirma que universe_filter del artefacto coincide con el filtro del scorer y que train_rows coincide con el parquet dentro de tolerancia"
    - "116 facilities con IQR=0 tienen iqr_guarded=1.0 (nunca se produce inf al dividir)"
  artifacts:
    - path: "src/fraud_detector/stats/tz_mapping.py"
      provides: "RAILS_TO_IANA dict con las 64 zonas Rails presentes en facility_tz.parquet"
      contains: "RAILS_TO_IANA"
    - path: "src/fraud_detector/stats/builder.py"
      provides: "FacilityStatsBuilder.build(train_df, tz_map, fid_currency) -> dict con fallback chain e IQR guarded"
      contains: "class FacilityStatsBuilder"
    - path: "src/fraud_detector/stats/validator.py"
      provides: "validate_universe_filter(stats, sample_df, tz_df) -> bool (verifica cobertura completa == len(tz_df))"
      contains: "def validate_universe_filter"
    - path: "scripts/build_facility_stats.py"
      provides: "Script offline que produce output/models/facility_stats_v1.json"
    - path: "output/models/facility_stats_v1.json"
      provides: "Artefacto de stats por facility versionado (schema_version='facility-stats-v1')"
      contains: "facility-stats-v1"
  key_links:
    - from: "src/fraud_detector/stats/builder.py"
      to: "src/fraud_detector/stats/tz_mapping.py"
      via: "import RAILS_TO_IANA para resolver iana_tz por facility"
      pattern: "RAILS_TO_IANA"
    - from: "scripts/build_facility_stats.py"
      to: "data/processed/train_features_enriched.parquet + output/revision/facility_tz.parquet"
      via: "pd.read_parquet al construir el artefacto"
      pattern: "read_parquet"
    - from: "src/fraud_detector/stats/builder.py"
      to: "iqr_guarded = max(iqr, 1.0)"
      via: "guardia contra IQR=0 almacenada en el artefacto"
      pattern: "iqr_guarded"
---

<objective>
Construir el artefacto de stats por facility `facility_stats_v1.json`: la raíz del grafo de dependencias de la Fase 1. Es un JSON versionado con median/IQR/mean/n, zona IANA, currency y fallback_level por facility, computado sobre el universo EXACTO del scorer. Incluye el mapa Rails→IANA completo (64 zonas) y un validator que confirma que el universo del artefacto coincide con el filtro del scorer.

Purpose: Sin este artefacto no existen las features de marco relativo (magnitud relativa a la facility, hora local IANA). Es lo que consumen 01-02 (FrameV1FeatureCalculator) y 01-03 (reentrenamiento). Satisface STATS-01 y STATS-02.
Output: `src/fraud_detector/stats/` (nuevo paquete: tz_mapping, builder, validator), `scripts/build_facility_stats.py`, y el artefacto materializado `output/models/facility_stats_v1.json`.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-artefacto-de-stats-y-feature-calculator/01-RESEARCH.md

# Prototipo de referencia — patrón exacto de facility_stats() y RAILS_TZ_TO_IANA (51 entradas)
@scripts/exp_frames_improvement.py
</context>

<constraints>
- NO tocar el scorer en vivo (SingleTransactionScorer, artifact_loader, features.py, features_enriched.py). Todo trabajo es código NUEVO aislado + script offline. La integración es Fase 3.
- El universo de cómputo es el filtro del scorer: `_peerdb_is_deleted=0 AND payment_method NOT IN ('reversal','free')`. El parquet `train_features_enriched.parquet` ya está filtrado a ese universo (extracción con FINAL); registrar el string de filtro en el metadata `universe_filter` del artefacto.
- venv: usar `./venv/bin/python` para todo. No instalar dependencias (zoneinfo/json/tzdata ya presentes).
- Idioma: comentarios de código en inglés (convención del repo); docstrings y mensajes de commit en inglés para código.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Mapa Rails→IANA completo (64 zonas) en tz_mapping.py</name>
  <files>src/fraud_detector/stats/__init__.py, src/fraud_detector/stats/tz_mapping.py, tests/test_facility_stats_builder.py</files>
  <action>
Crear el paquete `src/fraud_detector/stats/` con `__init__.py` vacío.

En `src/fraud_detector/stats/tz_mapping.py` definir un dict `RAILS_TO_IANA` que copie las 51 entradas del prototipo `scripts/exp_frames_improvement.py` (variable `RAILS_TZ_TO_IANA`, líneas 72-101) y AÑADA las 13 zonas faltantes con su IANA canónico:
  - "Baku": "Asia/Baku"
  - "Brussels": "Europe/Brussels"
  - "Darwin": "Australia/Darwin"
  - "Fiji": "Pacific/Fiji"
  - "Madrid": "Europe/Madrid"
  - "Magadan": "Asia/Magadan"
  - "Mazatlan": "America/Mazatlan"
  - "Montevideo": "America/Montevideo"
  - "Nairobi": "Africa/Nairobi"
  - "Santiago": "America/Santiago"
  - "Sarajevo": "Europe/Sarajevo"
  - "Sofia": "Europe/Sofia"
  - "UTC": "Etc/UTC"

Total: 64 entradas únicas (una por cada zona Rails presente en `output/revision/facility_tz.parquet`).

Añadir en el mismo archivo un guard de import: `assert len(RAILS_TO_IANA) == 64, "RAILS_TO_IANA debe cubrir las 64 zonas Rails presentes en facility_tz.parquet"`. Y una función `resolve_iana(rails_name: str) -> str` que retorne `RAILS_TO_IANA.get(rails_name, "Etc/UTC")` (fallback seguro para zonas Rails futuras/desconocidas).

Validar que TODOS los valores IANA son cargables por zoneinfo (evita typos que rompan el timezone conversion en 01-02). Crear el esqueleto de `tests/test_facility_stats_builder.py` con un test que itere `RAILS_TO_IANA.values()` y haga `ZoneInfo(v)` sin excepción, y que afirme cobertura completa contra el parquet:
  `set(pd.read_parquet("output/revision/facility_tz.parquet")["time_zone"].dropna().unique()).issubset(set(RAILS_TO_IANA))`.
  </action>
  <verify>./venv/bin/python -c "from fraud_detector.stats.tz_mapping import RAILS_TO_IANA; from zoneinfo import ZoneInfo; [ZoneInfo(v) for v in RAILS_TO_IANA.values()]; print(len(RAILS_TO_IANA))" imprime 64. Luego `./venv/bin/python -m pytest tests/test_facility_stats_builder.py -k tz_mapping -q` pasa.</verify>
  <done>RAILS_TO_IANA tiene 64 entradas, todos los IANA son cargables por zoneinfo, y el conjunto cubre el 100% de las zonas Rails de facility_tz.parquet (0 zonas sin mapear). resolve_iana() nunca lanza KeyError.</done>
</task>

<task type="auto">
  <name>Task 2: FacilityStatsBuilder con fallback chain e IQR guarded</name>
  <files>src/fraud_detector/stats/builder.py, tests/test_facility_stats_builder.py</files>
  <action>
Crear `src/fraud_detector/stats/builder.py` con `class FacilityStatsBuilder` (constante `MIN_N = 30`).

Método `build(self, train_df: pd.DataFrame, tz_map: dict[int, str], fid_currency: dict[int, str]) -> dict`:
  - `train_df` ya viene filtrado al universo del scorer; tiene columnas `amount`, `facility_id`, `currency`.
  - `tz_map`: dict {facility_id -> rails_tz_name} (de facility_tz.parquet). Resolver a IANA con `resolve_iana()`.
  - Para CADA facility en train_df: computar `n`, `median`, `mean`, `q1`, `q3`, `iqr = q3-q1`, `iqr_guarded = max(iqr, 1.0)`, `iana_tz = resolve_iana(tz_map.get(fid, ""))`.
  - fallback_level por entrada:
      - `n >= MIN_N` → `"facility"` (usa sus propios median/iqr_guarded/mean)
      - `n < MIN_N` pero existe currency_fallback para su moneda dominante → `"currency"` (magnitud tomada del currency fallback; retener n, iana_tz, e iqr raw para auditoría)
      - si no → `"global"`
  - Cobertura completa: TODA facility presente en tz_map (1876) debe tener entrada en `"facilities"`, incluso las que no están en train_df (sin historia de montos): esas reciben `fallback_level` en {"currency","global"}, con `median/mean/iqr` = None y `iana_tz` resuelto. Esto satisface el pitfall 3 del research (689 con stats vs 1876 con tz). El campo `iana_tz` DEBE estar presente en las 1876 para el real-time path. Regla dura: `len(stats["facilities"]) == len(tz_map)` (la iteración base es tz_map, NO train_df.groupby()).
  - Construir `currency_fallbacks`: median/iqr/iqr_guarded/mean/n por moneda para las 5 monedas más frecuentes en train (USD, CAD, MYR, HNL, NIO — ajustar según conteo real observado; incluir siempre USD). `fallback_level="currency"`.
  - Construir `global_fallback`: median/iqr/iqr_guarded/mean/n sobre todo train. `fallback_level="global"`.
  - Metadata: `schema_version="facility-stats-v1"`, `built_at` (UTC ISO), `universe_filter="_peerdb_is_deleted=0 AND payment_method NOT IN ('reversal','free') AND FINAL"`, `train_rows=len(train_df)`, `n_facilities` (== len(tz_map)), `min_n_threshold=30`.
  - Claves del dict `facilities` = `str(facility_id)` (JSON no admite int keys).
  - Todos los valores numéricos convertidos a `float`/`int` nativos de Python (no np.float64) para serialización JSON limpia.

Reglas de fórmula (deben coincidir con el prototipo `add_frame_features`): `iqr` bruto puede ser 0 y se almacena tal cual para auditoría; `iqr_guarded` es el que se usará en la fórmula z-score en 01-02. NO usar `iqr + 1e-6` como en el prototipo experimental — el research especifica `max(iqr, 1.0)` como guarda de producción (evita amplificar ruido en facilities de distribución casi-uniforme).

Añadir tests en `tests/test_facility_stats_builder.py`:
  - Fixture con un DataFrame sintético: facility A (n=100, spread normal), facility B (n=5, n<MIN_N), facility C (n=50, todos amount iguales → iqr=0), y un tz_map que incluye una facility D SIN filas en train_df (facility solo-tz, sin historia).
  - Test: A → fallback_level="facility", iqr_guarded==iqr (si iqr>=1).
  - Test: B → fallback_level in {"currency","global"}.
  - Test: C → iqr==0.0 pero iqr_guarded==1.0.
  - Test: D (solo-tz) → aparece en stats["facilities"] con iana_tz resuelto y fallback_level in {"currency","global"}.
  - Test: cobertura exacta → `len(stats["facilities"]) == len(tz_map)` (falla si el builder solo iteró train_df.groupby()).
  - Test: toda entrada tiene iana_tz no vacío.
  </action>
  <verify>./venv/bin/python -m pytest tests/test_facility_stats_builder.py -q pasa (todos los tests de builder, incluido el de cobertura len==len(tz_map)). Verificar manualmente: `./venv/bin/python -c "from fraud_detector.stats.builder import FacilityStatsBuilder; print(FacilityStatsBuilder.MIN_N)"` imprime 30.</verify>
  <done>FacilityStatsBuilder.build() produce un dict con schema_version, universe_filter, global_fallback, currency_fallbacks y facilities; `len(facilities) == len(tz_map)`; cada facility tiene iana_tz + iqr_guarded; facilities con n<30 marcadas 'currency'/'global'; facilities con IQR=0 tienen iqr_guarded=1.0. Tests unitarios verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Script de build + validator + materializar facility_stats_v1.json</name>
  <files>scripts/build_facility_stats.py, src/fraud_detector/stats/validator.py, output/models/facility_stats_v1.json, tests/test_facility_stats_builder.py</files>
  <action>
Crear `src/fraud_detector/stats/validator.py` con `validate_universe_filter(stats: dict, sample_df: pd.DataFrame, tz_df: pd.DataFrame) -> bool`:
  - `assert stats["universe_filter"] == "_peerdb_is_deleted=0 AND payment_method NOT IN ('reversal','free') AND FINAL"`.
  - `assert abs(stats["train_rows"] - len(sample_df)) / stats["train_rows"] < 0.001` (tolerancia 0.1%).
  - `assert stats["schema_version"] == "facility-stats-v1"`.
  - Cobertura de universo de facilities (cierra el gap del checker): computar `expected_n = tz_df["facility_id"].nunique()` (número de facilities con zona en facility_tz.parquet, esperado 1876) y `assert len(stats["facilities"]) == expected_n, f"cobertura incompleta: {len(stats['facilities'])} != {expected_n}"`. Esto detecta un builder que solo iterara `train_df.groupby()` (689 entradas) y evita el KeyError en real-time para facilities fuera del train window. Afirmar también `stats["n_facilities"] == expected_n`.
  - Validar cobertura de zona: `assert all(e.get("iana_tz") for e in stats["facilities"].values())` (ninguna facility sin zona no vacía).
  - Retornar True si todo pasa; lanzar AssertionError descriptivo si no.

Crear `scripts/build_facility_stats.py` (script offline ejecutable):
  - Leer `data/processed/train_features_enriched.parquet` (columnas: amount, facility_id, currency).
  - Leer `output/revision/facility_tz.parquet` → guardar el DataFrame `tz_df` y derivar `tz_map = {facility_id: time_zone}`.
  - Derivar `fid_currency = {facility_id: moneda_dominante}` desde train (moda de currency por facility).
  - Instanciar `FacilityStatsBuilder().build(train_df, tz_map, fid_currency)`.
  - Correr `validate_universe_filter(stats, train_df, tz_df)` ANTES de escribir; abortar si falla (esto garantiza que las 1876 facilities están cubiertas, no solo las 689 con historia).
  - Escribir `output/models/facility_stats_v1.json` con `json.dump(stats, f, indent=2)`.
  - Imprimir resumen: n_facilities totales (esperado 1876), cuántas con fallback_level='facility' (esperado ~689), 'currency', 'global'; cuántas con iqr=0 (esperado ~116).

Correr el script con `./venv/bin/python scripts/build_facility_stats.py` y materializar el artefacto real.

Añadir un test de integración en `tests/test_facility_stats_builder.py` (marcado skipif si el artefacto o los parquets no existen): cargar `output/models/facility_stats_v1.json`, afirmar schema_version, afirmar que existen entradas con fallback_level='facility', que TODAS las entradas tienen iana_tz, y que el conteo de iqr_guarded==1.0 es >0 (las ~116 con IQR=0). Leer `output/revision/facility_tz.parquet` y afirmar cobertura completa: `assert s["n_facilities"] == pd.read_parquet("output/revision/facility_tz.parquet")["facility_id"].nunique()` (esperado 1876) y `assert len(s["facilities"]) == s["n_facilities"]`. Correr `validate_universe_filter(s, train_df, tz_df)` contra el train parquet y el tz parquet. Esto alinea la verificación con la truth de must_haves (1876 con iana_tz) y protege el test de paridad de 01-02 contra KeyError en facilities fuera del train window.
  </action>
  <verify>`./venv/bin/python scripts/build_facility_stats.py` corre sin error y escribe output/models/facility_stats_v1.json. `./venv/bin/python -c "import json,pandas as pd; s=json.load(open('output/models/facility_stats_v1.json')); tz=pd.read_parquet('output/revision/facility_tz.parquet'); print(s['schema_version'], s['n_facilities'], len(s['facilities']), tz['facility_id'].nunique(), sum(1 for e in s['facilities'].values() if e['fallback_level']=='facility'), sum(1 for e in s['facilities'].values() if e.get('iqr')==0.0))"` imprime facility-stats-v1, n_facilities==len(facilities)==nunique (esperado 1876), ~689 con 'facility', ~116 con iqr=0. `./venv/bin/python -m pytest tests/test_facility_stats_builder.py -q` todo verde (incluye el assert de 1876 entradas).</verify>
  <done>output/models/facility_stats_v1.json existe, pasa validate_universe_filter (incluida la aserción `len(facilities) == n_facilities == tz_df.facility_id.nunique() == 1876`), contiene ~689 facilities con fallback_level='facility', el resto en 'currency'/'global', 116 con iqr_guarded=1.0, y las 1876 facilities de tz_map tienen iana_tz. El test de integración afirma `n_facilities == 1876`. STATS-01 y STATS-02 satisfechos.</done>
</task>

</tasks>

<verification>
- `./venv/bin/python -m pytest tests/test_facility_stats_builder.py -q` → todos verdes (incluye cobertura len(facilities)==len(tz_map) y n_facilities==1876).
- `./venv/bin/python -c "from fraud_detector.stats.tz_mapping import RAILS_TO_IANA; assert len(RAILS_TO_IANA)==64"` → sin error.
- El artefacto `output/models/facility_stats_v1.json` existe, es JSON válido, versionado, con universe_filter correcto.
- `validate_universe_filter(stats, train_df, tz_df)` pasa: `len(stats["facilities"]) == stats["n_facilities"] == tz_df["facility_id"].nunique()` (1876).
- Ninguna facility en `facilities` queda sin `iana_tz`; ninguna con n<30 queda sin fallback_level.
- 0 archivos existentes del scorer en vivo modificados (git diff no toca features.py, features_enriched.py, scorer.py, artifact_loader.py).
</verification>

<success_criteria>
- STATS-01: `facility_stats_v1.json` versionado con median/IQR/n/moneda/currency_group/zona IANA por facility, computado sobre el universo exacto del scorer, con validator que lo confirma (incluida cobertura completa de 1876 facilities).
- STATS-02: cadena de fallback (facility n<30 → currency → global) implementada, `fallback_level` registrado por entrada, ninguna facility sin stats.
- Cobertura: `len(facilities) == n_facilities == 1876` (todas las facilities con zona, no solo las 689 con historia de montos).
- IQR=0 guardado con `iqr_guarded=1.0` (nunca inf).
- Las 64 zonas Rails mapeadas a IANA cargable por zoneinfo.
</success_criteria>

<output>
Al completar, crear `.planning/phases/01-artefacto-de-stats-y-feature-calculator/01-01-SUMMARY.md` documentando: conteos reales del artefacto (n con 'facility'/'currency'/'global', n con iqr=0, n_facilities total == 1876), monedas elegidas para currency_fallbacks, y confirmación de que el scorer en vivo no fue tocado.
</output>
