---
phase: 01-artefacto-de-stats-y-feature-calculator
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/retrain_frame_v1.py
  - output/models/isolation_forest_frame_v1.joblib
  - output/models/scaler_frame_v1.joblib
  - output/models/model_metadata_frame_v1.json
  - output/frame_v1_bias_report.json
autonomous: false

must_haves:
  truths:
    - "Existe un modelo IsolationForest reentrenado sobre FS-frame-v1 (30 features) con la receta congelada (200,512,0.6,contamination=auto,seed=42) + RobustScaler(5,95) + clip ±10"
    - "El sesgo de monto top-5% en val set es <4× (baseline: 11.79×)"
    - "El off-hours local en val set es ~4-5% (baseline UTC: 29.78%)"
    - "El reentrenamiento es OFFLINE; el scorer en vivo no se toca (integración es Fase 3)"
    - "El metadata marca feature_version='frame-v1' para identificación futura por el artifact_loader"
    - "add_frame_features_from_artifact (batch, vectorizado) produce features idénticas (dentro de 1e-8) a FrameV1FeatureCalculator._compute_frame_features de 01-02 sobre una muestra del val set"
  artifacts:
    - path: "scripts/retrain_frame_v1.py"
      provides: "Script offline: añade frame features, entrena IF, computa gates de sesgo en val"
    - path: "output/models/isolation_forest_frame_v1.joblib"
      provides: "Modelo global reentrenado sobre FS-frame-v1"
    - path: "output/models/scaler_frame_v1.joblib"
      provides: "RobustScaler(5,95) fit en train frame-v1"
    - path: "output/models/model_metadata_frame_v1.json"
      provides: "Metadata con feature_version='frame-v1', feature_names, receta, métricas de sesgo"
      contains: "frame-v1"
    - path: "output/frame_v1_bias_report.json"
      provides: "Reporte de gates: top5_amount_ratio, off_hours_local_pct vs baseline"
  key_links:
    - from: "scripts/retrain_frame_v1.py"
      to: "src/fraud_detector/scoring/features_frame_v1.py (FRAME_V1_FEATURE_NAMES)"
      via: "usa la lista canónica de 30 features y la aritmética de marco"
      pattern: "FRAME_V1_FEATURE_NAMES"
    - from: "scripts/retrain_frame_v1.py"
      to: "output/models/facility_stats_v1.json"
      via: "carga stats para computar features de marco sobre train y val"
      pattern: "facility_stats_v1"
    - from: "top5_amount_ratio"
      to: "gate < 4x (baseline 11.79x de baseline_v0.json)"
      via: "comparación cuantitativa de sesgo, NO AUC"
      pattern: "top5"
---

<objective>
Reentrenar el modelo global de Isolation Forest sobre FS-frame-v1 (30 features de marco) usando la receta congelada, y validar los gates de reducción de sesgo sobre el val set completo (1.13M filas): top-5% monto <4× (baseline 11.79×) y off-hours local ~4-5% (baseline UTC 29.78%). Todo OFFLINE.

Purpose: Es la prueba cuantitativa de que la normalización de marco funciona sobre datos reales de validación, no solo en el prototipo de muestra. El gate es reducción de SESGO, nunca AUC contra pure_fraud (circularidad declarada). Satisface FRAME-04 y cierra la Fase 1.
Output: `scripts/retrain_frame_v1.py`, modelo + scaler + metadata frame-v1, y `output/frame_v1_bias_report.json`. Checkpoint humano final para inspeccionar el resultado del gate.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-artefacto-de-stats-y-feature-calculator/01-RESEARCH.md
@.planning/phases/01-artefacto-de-stats-y-feature-calculator/01-01-SUMMARY.md
@.planning/phases/01-artefacto-de-stats-y-feature-calculator/01-02-SUMMARY.md

# Aritmética de referencia de 01-02 (fuente de verdad; add_frame_features_from_artifact debe coincidir línea por línea)
@src/fraud_detector/scoring/features_frame_v1.py
# Test de paridad de 01-02 (se reutiliza como oráculo de paridad batch↔calculator)
@tests/test_parity_phase1.py
# Receta de entrenamiento + add_frame_features + evaluate (top5_amount_x_avg)
@scripts/exp_frames_improvement.py
# Baseline congelado (gate: 11.79x, off-hours UTC 29.78%)
@output/baseline_v0.json
</context>

<constraints>
- NO tocar el scorer en vivo ni el modelo de producción (isolation_forest_final.joblib, scaler_final.joblib, thresholds_v2.json). Producir artefactos NUEVOS con sufijo `_frame_v1`.
- venv: `./venv/bin/python`. Sin nuevas dependencias.
- Comparar el sesgo contra el baseline REAL congelado en 00-03: top-5% = 11.79× y off-hours UTC = 29.78% (de baseline_v0.json), NO contra el 15.7× del research (cohorte/feature-set distinto — decisión de STATE.md).
- Receta EXACTA (de isolation_forest_final.joblib, confirmada en research):
    IsolationForest(n_estimators=200, max_samples=512, max_features=0.6, contamination="auto", random_state=42, n_jobs=-1)
    Scaler: RobustScaler(quantile_range=(5.0, 95.0)); post-transform clip [-10, 10]; nan_to_num(nan=0, posinf=0, neginf=0) antes del fit.
- Feature set: FRAME_V1_FEATURE_NAMES (30) importado de features_frame_v1.py. No re-derivar la lista aquí.
- Paridad de aritmética (concern de primera clase): existen dos implementaciones de la aritmética de marco — el `_compute_frame_features` de 01-02 (real-time/batch fila-a-fila) y el `add_frame_features_from_artifact` de este plan (vectorizado sobre 1.13M filas). NO pueden divergir. La aritmética de add_frame_features_from_artifact debe replicar `_compute_frame_features` línea por línea (mismas fórmulas, mismo fallback chain, mismo iqr_guarded, mismo tz_localize UTC→astimezone). Se verifica con un test de paridad explícito (ver Task 1).
- Input: `train_features_enriched.parquet` (fit) y `val_features_enriched.parquet` (gate). Añadir las columnas de marco EN MEMORIA (patrón add_frame_features del prototipo) usando facility_stats_v1.json + el mapa tz. NO crear un nuevo parquet de entrenamiento.
- El gate es sobre el VAL set completo, no una muestra 1/20. Si RAM es limitante, procesar en chunks o usar columnas mínimas.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Script de reentrenamiento frame-v1 + artefactos del modelo</name>
  <files>scripts/retrain_frame_v1.py, output/models/isolation_forest_frame_v1.joblib, output/models/scaler_frame_v1.joblib, output/models/model_metadata_frame_v1.json</files>
  <action>
Crear `scripts/retrain_frame_v1.py` (offline):
  - Cargar `output/models/facility_stats_v1.json` y el tz_map de facility_tz.parquet.
  - Función `add_frame_features_from_artifact(df, stats)`: replica la aritmética de `add_frame_features` del prototipo (líneas 141-168 de exp_frames_improvement.py) PERO leyendo mean/median/iqr_guarded del artefacto (str(fid) keys) con fallback chain (facility→currency→global), y resolviendo iana_tz del artefacto por facility_id. IMPORTANTE: usar `iqr_guarded` del artefacto (no `iqr + 1e-6` del prototipo); amount_facility_ratio recalculado con el mean del artefacto (pitfall 7). Debe producir exactamente las 30 columnas de FRAME_V1_FEATURE_NAMES.
  - PARIDAD DE ARITMÉTICA (evita una tercera implementación divergente): la aritmética de `add_frame_features_from_artifact` debe ser IDÉNTICA línea por línea a `FrameV1FeatureCalculator._compute_frame_features` de 01-02 — mismas fórmulas (log_amount_fac, amount_facility_ratio, user_amount_24h_fac, user_debit_amount_30d_fac con denominador `fmean + 0.01`), mismo fallback chain (facility→currency→global), mismo `iqr_guarded`, misma resolución de hora local (`tz_localize("UTC").astimezone(ZoneInfo(iana_tz))`, OFF_HOURS={23,0,1,2,3,4,5,6}, dow 0=lunes) y mismo `off_hours_high_value_loc` (ratio>3). Al escribir cada fórmula, copiarla desde `_compute_frame_features` (no reinventarla desde el prototipo experimental). La única diferencia permitida es la forma (vectorizada pandas vs escalar), NUNCA la aritmética.
  - Sanity check: `assert set(FRAME_V1_FEATURE_NAMES).issubset(df.columns)` tras add_frame_features.
  - VERIFICACIÓN DE PARIDAD tras implementar add_frame_features_from_artifact: tomar una muestra del val set (reusar el mismo fixture/estilo de golden_rows de tests/test_parity_phase1.py, ≥100 filas ≥20 facilities), aplicar `add_frame_features_from_artifact` a esa muestra y, para cada fila, comparar el vector batch resultante (df[FRAME_V1_FEATURE_NAMES].iloc[i]) contra `FrameV1FeatureCalculator(stats, feature_engineer_path).calculate_from_row(row)`; afirmar `np.max(np.abs(batch - calculator)) < 1e-8` para las 30 features. Implementarlo como un helper reutilizable en el script (p.ej. `check_batch_calculator_parity(sample_df, stats)`) que también se pueda invocar desde pytest. Si falla, abortar el reentrenamiento (la divergencia silenciosa es el riesgo #1 de este plan).
  - Leer train_features_enriched.parquet, aplicar add_frame_features, extraer X_train = df[FRAME_V1_FEATURE_NAMES], `np.nan_to_num`.
  - `scaler = RobustScaler(quantile_range=(5.0,95.0)).fit(X_train)`; `X_train = np.clip(scaler.transform(X_train), -10, 10)`.
  - `model = IsolationForest(n_estimators=200, max_samples=512, max_features=0.6, contamination="auto", random_state=42, n_jobs=-1).fit(X_train)`.
  - Guardar `output/models/isolation_forest_frame_v1.joblib`, `output/models/scaler_frame_v1.joblib`.
  - Escribir `output/models/model_metadata_frame_v1.json` con: `feature_version="frame-v1"`, `feature_names=FRAME_V1_FEATURE_NAMES`, `n_features=30`, receta del IF, scaler config, `train_rows`, `built_at`, `stats_artifact="facility_stats_v1.json"`, `batch_calculator_parity_maxdiff` (el diff máx observado en la verificación de paridad), y placeholder para las métricas de sesgo (se rellenan en Task 2).
  - Imprimir tiempo de entrenamiento, confirmación de shapes, y el diff máx de la verificación de paridad batch↔calculator.
  </action>
  <verify>`./venv/bin/python scripts/retrain_frame_v1.py` (o la parte de entrenamiento) corre sin error e imprime el diff máx de paridad batch↔calculator (<1e-8). `./venv/bin/python -c "import joblib; m=joblib.load('output/models/isolation_forest_frame_v1.joblib'); print(m.n_estimators, m.max_samples, m.max_features)"` imprime 200 512 0.6. metadata JSON tiene feature_version='frame-v1', 30 feature_names y `batch_calculator_parity_maxdiff` < 1e-8. Además `./venv/bin/python -m pytest tests/test_parity_phase1.py -q` sigue verde (el calculator de 01-02 no cambió).</verify>
  <done>Modelo + scaler + metadata frame-v1 materializados. Receta idéntica al modelo final (200,512,0.6,auto,42). add_frame_features_from_artifact verificado contra FrameV1FeatureCalculator._compute_frame_features de 01-02 sobre ≥100 filas del val set (diff máx <1e-8, registrado en metadata). El modelo de producción (isolation_forest_final.joblib) no fue tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de sesgo sobre val set completo + reporte</name>
  <files>scripts/retrain_frame_v1.py, output/frame_v1_bias_report.json, output/models/model_metadata_frame_v1.json</files>
  <action>
Extender `scripts/retrain_frame_v1.py` con la evaluación de sesgo sobre val_features_enriched.parquet COMPLETO (1.13M filas):
  - Aplicar add_frame_features_from_artifact al val set; X_val = val[FRAME_V1_FEATURE_NAMES]; nan_to_num; clip con el scaler YA fit en train (no re-fit).
  - `scores = -model.decision_function(X_val_scaled)` (mayor = más anómalo).
  - Gate 1 — sesgo de monto top-5%:
      `k5 = int(len(scores)*0.05)`; `top5_idx = np.argsort(scores)[-k5:]`;
      `top5_amount_ratio = val["amount"].iloc[top5_idx].mean() / val["amount"].mean()`.
      Objetivo: < 4.0 (baseline 11.79×).
  - Gate 2 — off-hours local: `off_hours_local_pct = val["is_off_hours_loc"].mean()` (columna añadida por add_frame_features). Objetivo: ~4-5% (baseline UTC 29.78%).
      Reportar TAMBIÉN off_hours_utc_pct (de la columna is_off_hours del parquet) para contraste.
  - Escribir `output/frame_v1_bias_report.json` con: top5_amount_ratio, gate1_pass (bool, <4×), off_hours_local_pct, off_hours_utc_pct, gate2_pass (bool, 0.03<=pct<=0.07 como banda operativa de "~4-5%"), baseline_top5=11.79, baseline_offhours_utc=0.2978, n_val_rows, built_at. Marcar AUC como NO evaluado aquí (diagnóstico circular, no gate).
  - Actualizar model_metadata_frame_v1.json con el bloque `bias_metrics` (los mismos valores).
  - Imprimir un resumen legible: "top5: X.XX× (gate <4× : PASS/FAIL) | off-hours local: X.X% vs UTC XX.X% (gate ~4-5%: PASS/FAIL)".
  - Si un gate FALLA: NO abortar el script; escribir el reporte con el fallo. El paso de debug documentado (research open question 3) es verificar que las features de marco están bien computadas vía el test de paridad 01-02 y la verificación de paridad batch↔calculator de Task 1 — dejar esa nota en el reporte.
  </action>
  <verify>`./venv/bin/python scripts/retrain_frame_v1.py` corre end-to-end y escribe output/frame_v1_bias_report.json. `./venv/bin/python -c "import json; r=json.load(open('output/frame_v1_bias_report.json')); print('top5', round(r['top5_amount_ratio'],2), 'gate1', r['gate1_pass'], '| offhours_loc', round(r['off_hours_local_pct'],4), 'gate2', r['gate2_pass'])"` imprime valores; se espera top5<4 y off-hours local ~0.04-0.05.</verify>
  <done>output/frame_v1_bias_report.json existe con top5_amount_ratio, off_hours_local_pct/utc_pct, gate1_pass, gate2_pass, y comparación contra baseline (11.79×, 29.78%). Métricas escritas también en model_metadata_frame_v1.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Modelo global frame-v1 reentrenado sobre 30 features de marco (offline, sin tocar producción) y evaluado sobre el val set completo (1.13M filas). El reporte de sesgo `output/frame_v1_bias_report.json` contiene los dos gates cuantitativos de la Fase 1. La aritmética batch fue verificada contra el calculator de 01-02 (paridad <1e-8).
  </what-built>
  <how-to-verify>
1. Revisar `output/frame_v1_bias_report.json`:
   - `top5_amount_ratio` debe ser < 4.0 (baseline: 11.79×). Este es el gate primario de reducción de sesgo de monto.
   - `off_hours_local_pct` debe estar ~4-5% (0.04-0.05), muy por debajo del `off_hours_utc_pct` (~29.78%). Este es el gate de sesgo temporal.
2. Confirmar que `gate1_pass` y `gate2_pass` son ambos true.
3. Confirmar en `output/models/model_metadata_frame_v1.json` que `batch_calculator_parity_maxdiff` < 1e-8 (la aritmética batch no divergió del calculator de 01-02).
4. Si ALGÚN gate falla: la ruta de debug es correr `./venv/bin/python -m pytest tests/test_parity_phase1.py -q` (¿features de marco bien computadas?) y revisar la elección de currency_fallbacks del artefacto 01-01. NO promover el modelo si los gates fallan; decidir si se ablaciona (p.ej. añadir amount_fac_z, research open question 1) o se ajusta el artefacto.
5. Verificar que el modelo de producción sigue intacto: `output/models/isolation_forest_final.joblib` sin cambios (git status).

Decisión a tomar: ¿el frame-v1 reentrenado pasa ambos gates de sesgo y puede avanzar a Fase 2 (calibración segmentada)? Si sí, aprobar. Si no, describir el fallo para planear ablación/ajuste.
  </how-to-verify>
  <resume-signal>Escribir "approved" si ambos gates pasan, o describir el fallo (qué gate, valor observado) para decidir el siguiente paso (ablación / ajuste de artefacto).</resume-signal>
</task>

</tasks>

<verification>
- `./venv/bin/python scripts/retrain_frame_v1.py` corre end-to-end sin error.
- La verificación de paridad batch↔calculator (add_frame_features_from_artifact vs FrameV1FeatureCalculator.calculate_from_row de 01-02) sobre ≥100 filas del val set da diff máx <1e-8 (registrado en model_metadata_frame_v1.json como batch_calculator_parity_maxdiff).
- Artefactos frame-v1 existen: modelo, scaler, metadata (feature_version='frame-v1', 30 features), bias report.
- top5_amount_ratio < 4× y off_hours_local_pct ~4-5% en el val set completo.
- Modelo de producción intacto (git status limpio para isolation_forest_final.joblib / scaler_final.joblib / thresholds_v2.json).
- AUC contra pure_fraud NO usado como criterio (marcado diagnóstico).
</verification>

<success_criteria>
- FRAME-04: modelo global reentrenado sobre FS-frame-v1 pasa los gates de reducción de sesgo en validación (top-5% <4× vs 11.79×; off-hours local ~4-5% vs 29.78% UTC).
- Paridad de aritmética: add_frame_features_from_artifact idéntica (línea por línea) a _compute_frame_features de 01-02, verificada sobre ≥100 filas del val set con diff máx <1e-8. Sin tercera implementación divergente.
- Reentrenamiento estrictamente offline; scorer en vivo intacto.
- Metadata autodescriptiva con feature_version='frame-v1' para el artifact_loader de Fase 2.
- Checkpoint humano aprueba el resultado del gate antes de cerrar la fase.
</success_criteria>

<output>
Al completar, crear `.planning/phases/01-artefacto-de-stats-y-feature-calculator/01-03-SUMMARY.md`: valores reales de top5_amount_ratio y off-hours local/UTC, PASS/FAIL de cada gate, diff máx de la paridad batch↔calculator, comparación contra baseline (11.79×, 29.78%), y decisión del checkpoint humano (aprobado / ablación requerida).
</output>
