---
phase: 04-shadow-dual-run-y-validacion-de-sesgo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/clickhouse/migrations/02_anomaly_scores_frame_v1.sql
  - scorer/artifact_loader.py
  - scorer/shadow/__init__.py
  - scorer/shadow/dual_runner.py
  - scorer/batch/scorer.py
  - scorer/main.py
  - tests/test_shadow_dual_runner.py
  - tests/test_artifact_loader.py
autonomous: true

must_haves:
  truths:
    - "Cada pago scorable produce dos filas en anomaly_scores: scoring_mode='shadow_old' (model_version='IF-40-v1') y scoring_mode='shadow_new' (model_version='frame-v1')"
    - "El fallo de scoring de UN modelo no impide la escritura de la fila del otro modelo (fallo parcial aislado)"
    - "El segundo INSERT del mismo chunk NO queda deduplicado por ClickHouse (dedup token con prefijo shadow_old/shadow_new distinto)"
    - "La tabla anomaly_scores tiene calibration_segment, fallback_level, frame_flags con DEFAULT '' — filas IF-40 leen '' sin reescritura"
    - "El guardrail assert_write_target_is_safe sigue siendo la primera llamada de _insert_chunks; WRITE local intacto"
    - "El path IF-40 (len==40) recibe su contexto enriquecido por-pago sin duplicar los 6 queries bulk que comparte frame-v1"
  artifacts:
    - path: "docker/clickhouse/migrations/02_anomaly_scores_frame_v1.sql"
      provides: "ALTER TABLE ADD COLUMN IF NOT EXISTS de 3 columnas frame-v1"
      contains: "ADD COLUMN IF NOT EXISTS calibration_segment"
    - path: "scorer/shadow/dual_runner.py"
      provides: "ShadowDualRunner.score_pair — champion + challenger, nunca lanza excepción"
      min_lines: 40
    - path: "scorer/artifact_loader.py"
      provides: "_load_metadata acepta metadata_filename override; load_artifacts acepta metadata_filename"
      contains: "metadata_filename"
    - path: "scorer/batch/scorer.py"
      provides: "_INSERT_COLUMNS extendido a 26 columnas; modo dual con 2 filas/pago y dedup token prefijado"
      contains: "shadow-old-"
    - path: "scorer/main.py"
      provides: "lifespan carga champion + challenger cuando scoring_mode='shadow_dual'"
      contains: "shadow_dual"
  key_links:
    - from: "scorer/main.py lifespan"
      to: "load_artifacts(model_dir, metadata_filename='model_metadata_frame_v1.json')"
      via: "carga explícita del challenger frame-v1"
      pattern: "model_metadata_frame_v1"
    - from: "scorer/batch/scorer.py _insert_chunks (dual)"
      to: "ClickHouse insert con insert_deduplication_token"
      via: "tokens shadow-old-* y shadow-new-* distintos"
      pattern: "shadow-(old|new)-"
    - from: "ShadowDualRunner.score_pair"
      to: "scorer_champion.score y scorer_new.score con el mismo UserContext"
      via: "contexto factual compartido, no duplicado"
      pattern: "score_pair"
---

<objective>
Construir la infraestructura de dual-run: puntuar cada pago scorable con el champion (IF-40) y el challenger (frame-v1) simultáneamente, persistiendo DOS filas por pago en anomaly_scores. Migrar el DDL de ClickHouse con 3 columnas frame-v1, cargar ambos modelos en el lifespan, e implementar el ShadowDualRunner con dedup tokens distintos y aislamiento de fallo parcial.

Purpose: SHAD-01 — habilita la comparación directa champion vs frame-v1 por pago (mismo contexto, misma ventana), base para las métricas de sesgo de la Fase 4. Sin dual-run no hay dos scores comparables por pago.
Output: migración DDL idempotente, `scorer/shadow/dual_runner.py`, extensión del BatchScorer a modo dual (2 filas/pago, 26 columnas, tokens prefijados), lifespan con carga dual, y tests de infraestructura que corren sin datos shadow reales.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shadow-dual-run-y-validacion-de-sesgo/04-RESEARCH.md

# Fuentes de código a extender (auditadas)
@scorer/batch/scorer.py
@scorer/artifact_loader.py
@scorer/main.py
@docker/clickhouse/migrations/01_anomaly_scores_v2.sql
@docker/clickhouse/init/02_anomaly_scores.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración DDL (3 columnas frame-v1) + _load_metadata override + carga dual en lifespan</name>
  <files>docker/clickhouse/migrations/02_anomaly_scores_frame_v1.sql, scorer/artifact_loader.py, scorer/main.py, tests/test_artifact_loader.py</files>
  <action>
Tres piezas que habilitan cargar ambos modelos y persistir sus campos.

1. **Migración DDL** — crear `docker/clickhouse/migrations/02_anomaly_scores_frame_v1.sql` replicando EXACTAMENTE el patrón idempotente de `01_anomaly_scores_v2.sql`:
```sql
ALTER TABLE pbp_productionDB_optimized.anomaly_scores
    ADD COLUMN IF NOT EXISTS calibration_segment LowCardinality(String) DEFAULT '',
    ADD COLUMN IF NOT EXISTS fallback_level       LowCardinality(String) DEFAULT '',
    ADD COLUMN IF NOT EXISTS frame_flags          String DEFAULT '';
```
`ADD COLUMN IF NOT EXISTS` es idempotente y seguro de re-ejecutar sobre volumen existente (el init DDL solo corre en contenedores frescos — pitfall #4 del research). Las filas IF-40 existentes leen DEFAULT ''.

2. **`_load_metadata` override** en `scorer/artifact_loader.py`: cambiar la firma a `_load_metadata(model_dir: Path, metadata_filename: str = "model_metadata.json") -> dict` y usar `metadata_path = model_dir / metadata_filename` (línea 78). Los fallbacks legacy (final_feature_list.json → IF-40; final default → IF-31) se mantienen SOLO cuando `metadata_filename == "model_metadata.json"` (no romper retrocompat). Propagar el param en `load_artifacts(model_dir, metadata_filename="model_metadata.json")` pasándolo a `_load_metadata`. NO tocar `_validate_artifacts` ni el resto del flujo.

3. **Lifespan dual** en `scorer/main.py`: añadir a `ScorerSettings` los campos `shadow_champion_metadata: str = "model_metadata.json"`, `shadow_new_metadata: str = "model_metadata_frame_v1.json"`, y `scoring_mode: str = "active"` (valores: 'active' | 'shadow_dual'). En el lifespan, DESPUÉS de cargar `artifacts`/`scorer` (el activo, sin cambios), si `settings.scoring_mode == "shadow_dual"`:
```python
champion_artifacts = load_artifacts(model_dir, metadata_filename=settings.shadow_champion_metadata)
new_artifacts = load_artifacts(model_dir, metadata_filename=settings.shadow_new_metadata)
scorer_champion = SingleTransactionScorer(feature_engineer_path=str(model_dir / "feature_engineer.joblib"), ch_connector=read_ch_client, artifacts=champion_artifacts)
scorer_new = SingleTransactionScorer(feature_engineer_path=str(model_dir / "feature_engineer.joblib"), ch_connector=read_ch_client, artifacts=new_artifacts)
_deps._state["scorer_champion"] = scorer_champion
_deps._state["scorer_new"] = scorer_new
_deps._state["scoring_mode"] = settings.scoring_mode
```
Verificar tras la carga que `scorer_champion._model_version == 'IF-40-v1'` y `scorer_new._model_version == 'frame-v1'` (log de aserción; ambos artefactos viven en `output/models/`, distinguidos por metadata filename — pitfall #1). NO alterar la carga del scorer activo existente ni el guardrail de fingerprints.

Añadir tests en `tests/test_artifact_loader.py`: (a) `load_artifacts(model_dir, metadata_filename="model_metadata_frame_v1.json")` produce `feature_list` de 30 y `facility_stats`/`thresholds_segmented` no None; (b) el default `metadata_filename="model_metadata.json"` sigue produciendo el comportamiento IF-40 previo (regresión).
  </action>
  <verify>
`docker exec -i clickhouse clickhouse-client --multiquery < docker/clickhouse/migrations/02_anomaly_scores_frame_v1.sql` corre sin error y re-ejecutado no falla (idempotente); `docker exec clickhouse clickhouse-client -q "DESCRIBE pbp_productionDB_optimized.anomaly_scores FORMAT TabSeparated" | grep -E "calibration_segment|fallback_level|frame_flags"` muestra las 3 columnas. `source venv/bin/activate && pytest tests/test_artifact_loader.py -q` pasa. Si docker/clickhouse no está arriba, verificar el SQL con `clickhouse-client` no aplica: dejar documentado el comando en el SUMMARY runbook y validar solo los tests Python.
  </verify>
  <done>anomaly_scores tiene 26 columnas incluyendo calibration_segment/fallback_level/frame_flags (DEFAULT ''); load_artifacts carga frame-v1 explícitamente vía metadata_filename sin romper el default IF-40; lifespan expone scorer_champion (IF-40-v1) y scorer_new (frame-v1) en _state cuando scoring_mode='shadow_dual'.</done>
</task>

<task type="auto">
  <name>Task 2: ShadowDualRunner + modo dual en BatchScorer (2 filas/pago, 26 columnas, dedup prefijado, contexto IF-40)</name>
  <files>scorer/shadow/__init__.py, scorer/shadow/dual_runner.py, scorer/batch/scorer.py, tests/test_shadow_dual_runner.py</files>
  <action>
Núcleo del dual-run. Resuelve la open question #2 (contexto IF-40 vs bulk) explícitamente.

1. **`scorer/shadow/__init__.py`** vacío (paquete).

2. **`scorer/shadow/dual_runner.py`** — clase `ShadowDualRunner`:
```python
class ShadowDualRunner:
    def __init__(self, scorer_champion, scorer_new):
        self._champion = scorer_champion   # IF-40-v1
        self._new = scorer_new             # frame-v1
    def score_pair(self, payment, context_champion, context_new):
        # devuelve (ScoringResult champion, ScoringResult new). NUNCA lanza.
```
`score_pair` envuelve cada `scorer.score(payment, context=...)` en try/except; ante fallo de uno, loguea `logger.warning` con payment_id y devuelve un ScoringResult de error para ESE modelo (score=0.0, is_anomaly=False, risk_level="minimal", factors=[], model_version del scorer fallido, error string) sin afectar al otro. Loguea el delta abs de scores en debug. Es el aislamiento de fallo parcial (invariante SHAD-01).

3. **BatchScorer modo dual** en `scorer/batch/scorer.py`:
   - Extender `_INSERT_COLUMNS` con 3 entradas al final: `"calibration_segment"`, `"fallback_level"`, `"frame_flags"` (23 → 26). El orden coincide con el DDL migrado.
   - Añadir un runner opcional: `BatchScorer.__init__` acepta `scorer_shadow=None` (el challenger frame-v1). Cuando se provee, `score_batch` opera en modo dual.
   - **Contexto (open question #2 RESUELTA):** construir el `ctx_map` bulk una vez (6 queries, ya existente). El champion IF-40 (`len(feature_names)==40`) usa el path de contexto enriquecido por-pago que YA existe en `_score_all` (líneas 364-370: `scorer._context_provider.get_context(...)`). En modo dual, construir el `UserContext` enriquecido del champion una sola vez por pago y derivar el contexto de frame-v1 del mismo `ctx_map` bulk (frame-v1 usa 30 features, contexto factual compartido). NO duplicar los 6 queries bulk ni construir contexto dos veces para el mismo modelo. Documentar en docstring que el contexto es factual y compartido; el enriquecido IF-40 es superset del bulk.
   - Producir DOS filas por pago vía `ShadowDualRunner.score_pair`: fila champion con `scoring_mode='shadow_old'`, `model_version='IF-40-v1'`, y '' en las 3 columnas frame-v1; fila new con `scoring_mode='shadow_new'`, `model_version='frame-v1'`, y `calibration_segment`/`fallback_level`/`frame_flags` desde el ScoringResult (`str(r.calibration_segment or "")`, `str(r.fallback_level or "")`, `json.dumps(r.frame_flags) if r.frame_flags else ""`).
   - **Dedup tokens prefijados (pitfall #3):** en el `_insert_chunks` dual, insertar chunk_old con token `f"shadow-old-{cursor.isoformat()}-{cursor_end.isoformat()}-IF40v1-chunk-{i}"` y chunk_new con `f"shadow-new-{cursor.isoformat()}-{cursor_end.isoformat()}-framev1-chunk-{i}"`. Los prefijos DISTINTOS garantizan que el 2º INSERT del mismo pago no quede deduplicado. `assert_write_target_is_safe` SIGUE siendo la primera llamada de `_insert_chunks` (no tocar el guardrail; ambos inserts usan el mismo write_ch_client).
   - No romper el modo `active` existente (scorer_shadow=None → flujo actual intacto, 23 columnas... NOTA: con _INSERT_COLUMNS ahora en 26, el modo active debe pasar '' para las 3 nuevas columnas en cada row de `_score_all`; añadir los 3 '' al final de la fila en `_score_all` para mantener consistencia de columnas).
  </action>
  <verify>
`source venv/bin/activate && pytest tests/test_shadow_dual_runner.py tests/test_batch_scorer.py -q` pasa. Los tests nuevos (mocks, sin datos reales): (a) score_pair con champion que lanza excepción devuelve error-result champion + result new válido (aislamiento); (b) modo dual produce exactamente 2 filas por pago con scoring_mode shadow_old/shadow_new y model_version IF-40-v1/frame-v1; (c) los tokens de los dos inserts difieren por prefijo shadow-old-/shadow-new-; (d) cada fila tiene 26 valores en orden de _INSERT_COLUMNS; (e) assert_write_target_is_safe se invoca antes de cualquier insert (mock write client con guardrail que lanza si fingerprints iguales).
  </verify>
  <done>ShadowDualRunner puntúa champion+challenger sin lanzar excepción ante fallo parcial; BatchScorer en modo dual escribe 2 filas/pago (26 columnas) con tokens shadow-old-/shadow-new- distintos; contexto bulk compartido sin duplicar queries; guardrail WRITE intacto; modo active retrocompatible.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_shadow_dual_runner.py tests/test_artifact_loader.py tests/test_batch_scorer.py tests/test_if40_artifacts.py tests/test_scorer_frame_v1_dispatch.py -q` — toda la suite de scorer/shadow pasa (retrocompat IF-40 + frame-v1 dispatch intactos).
- `make lint` y `make format` limpios sobre los archivos modificados.
- Migración idempotente confirmada (re-ejecución sin error).
- Grep de seguridad: `grep -n "assert_write_target_is_safe" scorer/batch/scorer.py` sigue siendo la primera línea ejecutable de `_insert_chunks`.
</verification>

<success_criteria>
SHAD-01 cumplido a nivel infraestructura: el sistema puede producir 2 filas por pago (shadow_old/shadow_new) en un run de día-1 con datos sintéticos/pequeños, con dedup correcto, aislamiento de fallo parcial, y las 3 columnas frame-v1 persistidas. Retrocompat IF-40 y guardrail WRITE local verificados por tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shadow-dual-run-y-validacion-de-sesgo/04-01-SUMMARY.md`. Incluir en el runbook: el comando `docker exec -i clickhouse clickhouse-client --multiquery < docker/clickhouse/migrations/02_anomaly_scores_frame_v1.sql` y las env vars para activar el modo dual (`SCORING_MODE=shadow_dual`).
</output>
