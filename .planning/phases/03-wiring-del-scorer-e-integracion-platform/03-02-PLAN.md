---
phase: 03-wiring-del-scorer-e-integracion-platform
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/real_time_scoring_service.rb
  - /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/alert_manager.rb
  - /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/spec/services/anomaly_detection/real_time_scoring_service_spec.rb
  - /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/spec/services/anomaly_detection/alert_manager_spec.rb
autonomous: false

must_haves:
  truths:
    - "RealTimeScoringService#scorable? excluye payment_method 'free' además de 'reversal' — alineado con la exclusión SQL del batch scorer (PLAT-03)"
    - "AlertManager persiste calibration_segment, fallback_level, frame_flags y feature_frame_version en la metadata JSON de la alerta (PLAT-02)"
    - "RealTimeScoringService#create_alert propaga los campos frame-v1 recibidos del scorer al hash de AlertManager"
    - "Los campos nuevos son nil-safe: cuando el scorer responde en modo IF-40 (sin frame-v1), .compact los elimina y el alert legacy no cambia"
    - "Un pago de prueba puntúa end-to-end (Rails → scorer → alerta con metadata completa) en entorno de test, sin errores (PLAT-02/E2E)"
  artifacts:
    - path: "platform/packs/anomaly_detection/app/services/anomaly_detection/real_time_scoring_service.rb"
      provides: "scorable? excluye 'free'; create_alert propaga campos frame-v1"
      contains: "payment_method == \"free\""
    - path: "platform/packs/anomaly_detection/app/services/anomaly_detection/alert_manager.rb"
      provides: "build_metadata con 4 campos nuevos"
      contains: "calibration_segment"
    - path: "platform/packs/anomaly_detection/spec/services/anomaly_detection/real_time_scoring_service_spec.rb"
      provides: "specs de scorable? free + propagación de campos"
      contains: "when payment is free"
    - path: "platform/packs/anomaly_detection/spec/services/anomaly_detection/alert_manager_spec.rb"
      provides: "specs de persistencia de metadata frame-v1"
      contains: "calibration_segment"
  key_links:
    - from: "RealTimeScoringService#create_alert"
      to: "AlertManager.call alerts_data"
      via: "hash con calibration_segment/fallback_level/frame_flags/feature_frame_version desde score_result"
      pattern: "calibration_segment"
    - from: "AlertManager#build_metadata"
      to: "Alert.metadata (columna JSON)"
      via: "hash .compact persistido en Alert.create!"
      pattern: "build_metadata"
    - from: "RealTimeScoringService#scorable?"
      to: "batch scorer SQL (payment_method NOT IN ('reversal','free'))"
      via: "return false if payment_method == 'free'"
      pattern: "free"
---

<objective>
Integrar el pack Rails `anomaly_detection` con el contrato frame-v1: (1) alinear `scorable?` entre batch y real-time excluyendo `payment_method == "free"` (el batch ya lo excluye en SQL, real-time no); (2) persistir los campos frame-v1 (`calibration_segment`, `fallback_level`, `frame_flags`, `feature_frame_version`) en la metadata JSON de la alerta; (3) verificar un scoring end-to-end de prueba (Rails → scorer → alerta) en entorno de test.

Purpose: Cierra PLAT-02 y PLAT-03. Sin migración — la tabla `anomaly_detection_alerts` ya tiene columna `metadata JSON`; extender el hash `build_metadata` basta (columnas dedicadas se difieren a Fase 4). PLAT-01 queda reconciliado en 03-01: el scorer resuelve la zona IANA autónomamente desde `facility_stats_v1.json`, por lo que Rails NO necesita enviar `facility_time_zone_iana` — se documenta como metadata OPCIONAL, no dependencia.

Output: `scorable?` alineado con exclusión de `free`; `AlertManager#build_metadata` extendido; `RealTimeScoringService#create_alert` propagando campos; specs RSpec (TDD) para cada cambio; verificación E2E en test.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/phases/03-wiring-del-scorer-e-integracion-platform/03-RESEARCH.md

# Contrato del scorer (fuente de los campos que Rails consume) — de 03-01:
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scorer/schemas.py

# Código Rails a modificar (leer antes de editar):
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/real_time_scoring_service.rb
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/alert_manager.rb

# Specs existentes (extender, no reescribir):
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/spec/services/anomaly_detection/real_time_scoring_service_spec.rb
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/spec/services/anomaly_detection/alert_manager_spec.rb
</context>

<constraints>
- **TDD obligatorio (regla platform):** para cada cambio de comportamiento, escribir primero un spec que falle, verificar que falla, implementar, verificar que pasa. Ejecutar la suite del pack al final.
- **PLAT-01 reconciliado — NO añadir `facility_time_zone_iana` como dependencia:** el scorer lo resuelve solo desde el artefacto (03-01). No modificar `build_payload` para hacerlo obligatorio. Si se decide enviarlo como metadata opcional, debe usar la columna DB-safe `facility.tzinfo_identifier` (NUNCA `facility.time_zone_identifier`, que puede lanzar `NoMethodError` si `time_zone` es nil) — pero el scorer lo ignora. El criterio real (features de hora local correctas + `frame_flags.timezone_missing` observable) se cumple en 03-01.
- **Sin migración en Fase 3:** persistir en `metadata JSON` vía `build_metadata`. Columnas dedicadas SQL = Fase 4. El success criterion del roadmap "consultables en ClickHouse" se satisface parcialmente vía JSON en MySQL para Fase 3 (test/integration); la consultabilidad directa en ClickHouse depende del INSERT batch que NO se toca aquí (DDL sin columnas — Fase 4).
- **nil-safe:** `.compact` en `build_metadata` ya elimina keys nil. En modo IF-40 el scorer responde sin los campos frame-v1 → nil → eliminados → alert legacy idéntico. NO romper alertas existentes.
- **`feature_frame_version`:** reusar el valor de `feature_version` (`"frame-v1"`) — NO se añade campo separado en el scorer (decisión open-question 1). En Rails, mapear `"feature_frame_version" => score_result["feature_version"]`.
- **Idempotencia de alertas:** `create_alert` en `AlertManager` ya deduplica por `(payment_id, alert_type)` — no romper esa lógica.
- **NO shadow/dual-run** (Fase 4). Esta fase deja Rails cableado y probado en test.
- `bundle exec` prefijo en todos los comandos. `Time.current` nunca `Time.now`. RuboCop `-A` sobre archivos cambiados.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: scorable? excluye 'free' (PLAT-03) — TDD</name>
  <files>/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/real_time_scoring_service.rb, /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/spec/services/anomaly_detection/real_time_scoring_service_spec.rb</files>
  <action>
RED: en `real_time_scoring_service_spec.rb`, añadir un `context "when payment is free"` que construya un payment con `payment_method: "free"`, invoque el service, y espere que `scorable?` sea false — concretamente que `@client.score_single` NO se llame y que el resultado sea `nil` (mismo patrón que el context existente de `reversal`). Verificar que falla (hoy `free` SÍ se puntúa).

GREEN: en `real_time_scoring_service.rb::scorable?` (líneas 30-35), añadir la guarda de `free` junto a la de `reversal`:

```ruby
def scorable?
  return false if @payment.payment_method == "reversal"
  return false if @payment.payment_method == "free"
  return false if @payment.status.present? && @payment.status.in?(Payment::REFUNDED_STATUSES)

  Setting.get("realtime_scoring_enabled") != "false"
end
```

Justificación (comentario breve): alinea el universo real-time con la exclusión SQL del batch scorer (`payment_method NOT IN ('reversal','free')`). Los pagos `free` producen features sin sentido (amount 0, ratios degenerados).

Verificar que el spec pasa y que el context existente de `reversal` sigue verde.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/platform && bundle exec rspec packs/anomaly_detection/spec/services/anomaly_detection/real_time_scoring_service_spec.rb -e "free" -e "reversal"</verify>
  <done>`scorable?` retorna false para `payment_method == "free"`; el pago free NO llega al scorer; specs de `free` y `reversal` PASS. Universo `scorable?` alineado batch↔real-time.</done>
</task>

<task type="auto">
  <name>Task 2: Persistir metadata frame-v1 en AlertManager + create_alert (PLAT-02) — TDD</name>
  <files>/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/alert_manager.rb, /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/real_time_scoring_service.rb, /Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/spec/services/anomaly_detection/alert_manager_spec.rb</files>
  <action>
RED: en `alert_manager_spec.rb`, añadir un test que pase `alerts_data` incluyendo `"calibration_segment" => "facility:1234"`, `"fallback_level" => "facility"`, `"frame_flags" => {"timezone_missing" => false, "currency_missing" => false, "currency_unknown" => false}`, `"feature_frame_version" => "frame-v1"`, y afirmar que la `Alert` creada tiene esos 4 campos dentro de `alert.metadata` (JSON). Añadir también un test de retrocompat: `alerts_data` SIN esos campos (modo IF-40) produce una alert cuya `metadata` NO contiene esas keys (por `.compact`) y sigue teniendo `model_version`/`feature_version`/`threshold_version`. Verificar RED.

GREEN en `alert_manager.rb::build_metadata` (líneas 60-71), añadir los 4 campos al hash (ver 03-RESEARCH.md "AlertManager#build_metadata extendido"):

```ruby
def build_metadata(data)
  {
    user_id: data["user_id"],
    amount_usd: data["amount_usd"],
    percentile: data["percentile"],
    is_anomaly: data["is_anomaly"],
    factors: data["factors"],
    model_version: data["model_version"],
    feature_version: data["feature_version"],
    threshold_version: data["threshold_version"],
    calibration_segment: data["calibration_segment"],
    fallback_level: data["fallback_level"],
    frame_flags: data["frame_flags"],
    feature_frame_version: data["feature_frame_version"]
  }.compact
end
```

GREEN en `real_time_scoring_service.rb::create_alert` (líneas 75-93), añadir las 4 keys al hash de `alerts_data`, mapeando `feature_frame_version` al `feature_version` del scorer (open-question 1 resuelta: reusar `feature_version`):

```ruby
"calibration_segment" => score_result["calibration_segment"],
"fallback_level" => score_result["fallback_level"],
"frame_flags" => score_result["frame_flags"],
"feature_frame_version" => score_result["feature_version"]
```

Nota: en modo IF-40 estas keys son `nil` → `.compact` en `build_metadata` las elimina → alert legacy idéntico (retrocompat).

Verificar que ambos tests (frame-v1 persistido + IF-40 sin keys) pasan y que los specs existentes de `alert_manager_spec.rb` siguen verdes.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/platform && bundle exec rspec packs/anomaly_detection/spec/services/anomaly_detection/alert_manager_spec.rb</verify>
  <done>`Alert.metadata` contiene `calibration_segment`/`fallback_level`/`frame_flags`/`feature_frame_version` cuando el scorer responde en frame-v1; en modo IF-40 esas keys se omiten (`.compact`) y la alert legacy no cambia; toda la suite de `alert_manager_spec.rb` PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Wiring completo Rails↔scorer para frame-v1: `scorable?` excluye `free`, `AlertManager` persiste 4 campos nuevos en `metadata JSON`, `create_alert` los propaga desde la respuesta del scorer. Combinado con 03-01 (scorer despacha frame-v1 + router propaga los campos), la tubería real-time end-to-end está cableada.
  </what-built>
  <how-to-verify>
Verificación E2E en ENTORNO DE TEST (no producción; el shadow real es Fase 4). Correr la corrida de integración local (READ prod / WRITE local; platform Rails en host, infra en docker — ver MEMORY project_local_integration_run):

1. Levantar el scorer apuntado al model dir frame-v1 (o con `Artifacts` frame-v1 cargado) de modo que `_is_frame_v1` sea true. Confirmar en el log de arranque del scorer que reporta modo frame-v1.
2. En consola Rails (test/dev), tomar un pago scorable real de la facility de prueba y ejecutar `AnomalyDetection::RealTimeScoringService.new(payment: payment).call`.
3. Afirmar que la respuesta incluye `calibration_segment`, `fallback_level`, `frame_flags` no-null.
4. Si se genera alerta, inspeccionar `Alert.last.metadata` y confirmar las 4 keys nuevas presentes con valores del scorer.
5. Tomar un pago con `payment_method: "free"` y confirmar que `RealTimeScoringService...call` retorna `nil` (no puntúa).
6. Confirmar que un pago que cae en una facility conocida NO tiene `frame_flags["timezone_missing"] == true` (zona resuelta desde artefacto), y que una facility desconocida sí lo marca sin lanzar excepción.

Expected: scoring end-to-end sin errores, metadata completa persistida, `free` excluido, `timezone_missing` observable. NINGÚN escritura de shadow/dual-run (eso es Fase 4).
  </how-to-verify>
  <resume-signal>Escribe "approved" si el E2E de prueba pasa con metadata completa, o describe los campos faltantes/errores observados.</resume-signal>
</task>

</tasks>

<verification>
- `bundle exec rspec packs/anomaly_detection/spec/services/anomaly_detection/real_time_scoring_service_spec.rb packs/anomaly_detection/spec/services/anomaly_detection/alert_manager_spec.rb` — toda la suite del pack afectada PASS.
- `bundle exec rubocop -A` sobre los 2 archivos de servicio cambiados — sin ofensas.
- Grep de sanidad: `grep -n 'payment_method == "free"' packs/anomaly_detection/app/services/anomaly_detection/real_time_scoring_service.rb` — presente.
- `build_payload` NO fue modificado para hacer obligatorio `facility_time_zone_iana` (PLAT-01 reconciliado): `git diff` de `real_time_scoring_service.rb` no toca `build_payload` (salvo opcionalmente añadir tzinfo_identifier como metadata no-dependiente).
- Checkpoint E2E aprobado por el usuario.
</verification>

<success_criteria>
- PLAT-03: `scorable?` excluye `reversal` y `free` de forma idéntica al batch; reembolsados marcados sin afectar el scoring en vivo.
- PLAT-02: `AlertManager` persiste los 4 campos en `metadata JSON`; retrocompat IF-40 vía `.compact`.
- PLAT-01 (reconciliado): la resolución IANA es autónoma en el scorer (03-01); Rails no depende de enviar `facility_time_zone_iana`; `frame_flags.timezone_missing` observable.
- E2E de prueba (Rails → scorer → alerta) pasa en test sin errores y con metadata completa.
- Sin migración, sin shadow/dual-run (diferidos a Fase 4).
</success_criteria>

<output>
After completion, create `/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/phases/03-wiring-del-scorer-e-integracion-platform/03-02-SUMMARY.md`.
</output>
