---
phase: 03-wiring-del-scorer-e-integracion-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/classifier.py
  - /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/scorer.py
  - /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scorer/routers/score.py
  - /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/tests/test_scorer_frame_v1_dispatch.py
autonomous: true

must_haves:
  truths:
    - "El scorer despacha a FrameV1FeatureCalculator + SegmentedThresholdClassifier cuando facility_stats y thresholds_segmented están presentes en Artifacts"
    - "El scorer mantiene el path IF-40 legacy (EnrichedFeatureCalculator + ThresholdClassifier) intacto cuando los artefactos frame-v1 NO están presentes"
    - "La respuesta del endpoint /score incluye calibration_segment, fallback_level y frame_flags poblados en modo frame-v1"
    - "frame_flags.timezone_missing=true cuando la facility no está en el artefacto (fallback Etc/UTC); nunca lanza excepción"
    - "El scorer resuelve la zona IANA autónomamente desde facility_stats_v1.json — no depende de que Rails envíe facility_time_zone_iana (PLAT-01 reconciliado)"
  artifacts:
    - path: "src/fraud_detector/scoring/classifier.py"
      provides: "ScoringResult con 3 campos nuevos (calibration_segment, fallback_level, frame_flags) con default None"
      contains: "calibration_segment"
    - path: "src/fraud_detector/scoring/scorer.py"
      provides: "Dispatch frame-v1 por presencia de artefactos + rama score() que llama SegmentedThresholdClassifier y construye frame_flags"
      contains: "_is_frame_v1"
    - path: "scorer/routers/score.py"
      provides: "Propagación de calibration_segment/fallback_level/frame_flags al ScoreResponse"
      contains: "frame_flags"
    - path: "tests/test_scorer_frame_v1_dispatch.py"
      provides: "Tests de dispatch, retrocompat IF-40, y poblado de campos nuevos"
      contains: "def test_"
  key_links:
    - from: "src/fraud_detector/scoring/scorer.py::SingleTransactionScorer.__init__"
      to: "artifacts.facility_stats / artifacts.thresholds_segmented"
      via: "condición de dispatch por presencia (is not None)"
      pattern: "facility_stats is not None"
    - from: "src/fraud_detector/scoring/scorer.py::SingleTransactionScorer.score"
      to: "SegmentedThresholdClassifier.classify"
      via: "5-tuple (is_anomaly, risk_level, percentile, fallback_level, calibration_segment) con facility_id+currency"
      pattern: "classify\\(.*facility_id"
    - from: "scorer/routers/score.py::score_single"
      to: "ScoreResponse"
      via: "result.calibration_segment / result.fallback_level / FrameFlags(**result.frame_flags)"
      pattern: "calibration_segment=result"
---

<objective>
Cablear el scorer en vivo para que despache al `FrameV1FeatureCalculator` + `SegmentedThresholdClassifier` cuando los artefactos frame-v1 (`facility_stats`, `thresholds_segmented`) están presentes en `Artifacts`, manteniendo el path IF-40 legacy intacto cuando no lo están. Extender `ScoringResult` con los 3 campos del contrato de Fase 2 (`calibration_segment`, `fallback_level`, `frame_flags`) y propagarlos desde el router al `ScoreResponse`.

Purpose: Cierra el wiring que Fase 1 y Fase 2 dejaron probado pero desconectado — el scorer YA carga los artefactos vía `artifact_loader`, el contrato `ScoreResponse` YA define los campos como Optional, pero el `SingleTransactionScorer` nunca los usa ni el router los llena. Es la primera fase que toca el scorer en vivo; debe ser retrocompatible (IF-40 debe seguir funcionando byte-a-byte si los artefactos frame-v1 no están).

Output: `ScoringResult` extendido; `SingleTransactionScorer` con tercera rama de dispatch y rama frame-v1 en `score()`; router que propaga los 3 campos; suite de tests que prueba dispatch, retrocompat IF-40 y poblado de campos.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/ROADMAP.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/STATE.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/phases/03-wiring-del-scorer-e-integracion-platform/03-RESEARCH.md

# Código a modificar (leer antes de editar):
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/scorer.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/classifier.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scorer/routers/score.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scorer/schemas.py

# Componentes ya listos (leer para firmas, NO modificar):
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/features_frame_v1.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scorer/artifact_loader.py
</context>

<constraints>
- **Retrocompat IF-40 obligatoria:** los 3 campos nuevos de `ScoringResult` llevan `default=None`. El path IF-40 (`EnrichedFeatureCalculator` + `ThresholdClassifier`, `len(feature_names)==40`) NO cambia su comportamiento. El default `output/models/model_metadata.json` es IF-40 (`stats_artifact: None`) → el scorer en vivo sigue en IF-40 salvo que se apunte al model dir frame-v1.
- **Dispatch por presencia de artefactos, NO por conteo de features:** condición primaria `artifacts.facility_stats is not None and artifacts.thresholds_segmented is not None`. NUNCA `elif len==30` (un futuro modelo de 30 features sin stats tomaría frame-v1 por error).
- **NO tocar `_INSERT_COLUMNS` del batch scorer:** el DDL de ClickHouse `anomaly_scores` (`docker/clickhouse/init/02_anomaly_scores.sql`) NO tiene columnas para `calibration_segment`/`fallback_level`/`frame_flags`/`feature_frame_version`. Extenderlas rompería el INSERT. La persistencia batch de estos campos es Fase 4. Esta fase solo cablea el path real-time (`/score` → `ScoreResponse`).
- **flag correcto = `timezone_missing`** (no `timezone_invalid`): `_lookup_facility` nunca lanza; usa fallback `Etc/UTC`. `timezone_missing=true` sii la facility no está en el artefacto.
- **currency normalizada antes del classifier:** `currency = (payment.get("currency") or "USD").upper()` — evita que `None` colapse todo a segmento global.
- **NO activar shadow/dual-run** (Fase 4). Esta fase deja el scorer wired y probado en test.
- Sin lenguaje causal en comentarios/docstrings; contexto no supervisado.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Extender ScoringResult con 3 campos opcionales (RED→GREEN)</name>
  <files>/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/classifier.py, /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/tests/test_scorer_frame_v1_dispatch.py</files>
  <action>
En `classifier.py`, extender el `@dataclass ScoringResult` (líneas 47-58) añadiendo 3 campos trailing con default `None` para preservar retrocompat:

```python
from typing import List, Optional  # asegurar Optional importado (línea 12 ya tiene Tuple)

@dataclass
class ScoringResult:
    score: float
    is_anomaly: bool
    risk_level: str
    percentile: float
    factors: List[dict] = field(default_factory=list)
    model_version: str = "IF-31-v1"
    feature_version: str = "base-31"
    threshold_version: str = "v1"
    # Fase 3 — poblados SOLO en el path frame-v1; None en IF-40 legacy.
    calibration_segment: Optional[str] = None
    fallback_level: Optional[str] = None
    frame_flags: Optional[dict] = None  # dict (no FrameFlags) para no acoplar classifier a pydantic
```

En `tests/test_scorer_frame_v1_dispatch.py` (crear archivo), escribir primero el test RED de este contrato:
- `test_scoring_result_defaults_none`: construir `ScoringResult(score=0.1, is_anomaly=False, risk_level="minimal", percentile=0.4)` y afirmar que `calibration_segment is None`, `fallback_level is None`, `frame_flags is None`. Esto verifica que el path IF-40 (que construye `ScoringResult` sin estos kwargs) sigue funcionando sin `TypeError`.

Seguir el estilo de tests existentes en `tests/test_if40_artifacts.py` (pytest, imports desde `fraud_detector.scoring...`). Correr el test: RED antes de tocar el dataclass, GREEN después.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector && source venv/bin/activate && pytest tests/test_scorer_frame_v1_dispatch.py::test_scoring_result_defaults_none -x -q</verify>
  <done>`ScoringResult` acepta y omite los 3 campos nuevos; `test_scoring_result_defaults_none` PASS; construir `ScoringResult` sin los kwargs nuevos NO lanza `TypeError` (retrocompat IF-40 probada).</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch frame-v1 en SingleTransactionScorer (__init__ + score) con TDD</name>
  <files>/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/scorer.py, /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/tests/test_scorer_frame_v1_dispatch.py</files>
  <action>
RED primero: en `tests/test_scorer_frame_v1_dispatch.py` añadir tests que construyan `SingleTransactionScorer(artifacts=...)` con un `Artifacts` frame-v1 (usar `load_artifacts` apuntado a un model dir frame-v1, replicando el patrón de `tests/test_artifact_loader.py` que usa `tmp_path` con `model_metadata.json` renombrado desde `model_metadata_frame_v1.json` — ver decisión [02-03] en STATE.md). Tests:
- `test_dispatch_selects_frame_v1_when_artifacts_present`: afirmar `scorer._is_frame_v1 is True` y que `scorer._classifier` es `SegmentedThresholdClassifier` y `scorer._feature_calc` es `FrameV1FeatureCalculator`.
- `test_dispatch_stays_if40_when_stats_absent`: cargar el `Artifacts` IF-40 (default `output/models/`, `facility_stats=None`) → afirmar `scorer._is_frame_v1 is False`, `_classifier` es `ThresholdClassifier`.
- `test_frame_v1_score_populates_fields`: con un `context` pre-computado (`UserContext`) y un payment mínimo, llamar `scorer.score(payment, context=ctx)` y afirmar que `result.calibration_segment` es un string (`facility:...`|`currency:...`|`global`), `result.fallback_level in {"facility","currency","global"}`, `result.frame_flags` es dict con keys `timezone_missing`/`currency_missing`/`currency_unknown`.
- `test_frame_flags_timezone_missing_for_unknown_facility`: payment con `facility_id` que NO está en `facility_stats` → `result.frame_flags["timezone_missing"] is True` y NO lanza excepción.

GREEN: en `scorer.py::__init__`, sustituir el bloque de selección de `_feature_calc` (líneas 64-70) por dispatch por presencia de artefactos ANTES del `elif len==40`. Guardar `self._is_frame_v1`. Importar `FrameV1FeatureCalculator` y `SegmentedThresholdClassifier` (import diferido dentro del `if` para no romper imports en path IF-40). Estructura (ver 03-RESEARCH.md "Detección frame-v1 en SingleTransactionScorer.__init__"):

```python
self._is_frame_v1 = False
if artifacts is not None and getattr(artifacts, "facility_stats", None) is not None \
        and getattr(artifacts, "thresholds_segmented", None) is not None:
    from fraud_detector.scoring.features_frame_v1 import FrameV1FeatureCalculator
    from fraud_detector.scoring.classifier import SegmentedThresholdClassifier
    self._feature_calc = FrameV1FeatureCalculator(
        facility_stats=artifacts.facility_stats,
        feature_engineer_path=feature_engineer_path,
    )
    self._classifier = SegmentedThresholdClassifier(artifacts.thresholds_segmented)
    self._is_frame_v1 = True
elif len(self._feature_names) == 40:
    self._feature_calc = EnrichedFeatureCalculator(...)  # existente sin cambios
else:
    self._feature_calc = SingleFeatureCalculator(feature_engineer_path)  # existente
```

IMPORTANTE: cuando `_is_frame_v1`, el `self._classifier` ya es `SegmentedThresholdClassifier`; NO reasignar `ThresholdClassifier` en la rama de artefactos (el bloque `if artifacts is not None` en líneas 46-51 asigna `ThresholdClassifier` — moverlo/condicionarlo para que frame-v1 no lo sobreescriba, o reasignar después del dispatch).

En `scorer.py::score()`, tras `features = self._feature_calc.calculate(payment, context)` y `raw_score, X_scaled = self.score_features(features)` (líneas 112-114), bifurcar la clasificación (ver 03-RESEARCH.md "scorer.score() — rama frame-v1"):

```python
calibration_segment = None
fallback_level = None
frame_flags = None
if self._is_frame_v1:
    facility_id = int(payment.get("facility_id", 0))
    currency = (payment.get("currency") or "USD").upper()
    is_anomaly, risk_level, percentile, fallback_level, calibration_segment = \
        self._classifier.classify(raw_score, facility_id=facility_id, currency=currency)
    fid_str = str(facility_id)
    tz_missing = self._feature_calc._stats["facilities"].get(fid_str) is None
    frame_flags = {
        "timezone_missing": bool(tz_missing),
        "currency_missing": payment.get("currency") is None,
        "currency_unknown": False,
    }
else:
    is_anomaly, risk_level, percentile = self._classifier.classify(raw_score)
```

Pasar los 3 campos al `ScoringResult(...)` de retorno (líneas 125-134): `calibration_segment=calibration_segment, fallback_level=fallback_level, frame_flags=frame_flags`.

Correr los 4 tests: RED antes, GREEN después.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector && source venv/bin/activate && pytest tests/test_scorer_frame_v1_dispatch.py -x -q && pytest tests/test_if40_artifacts.py tests/test_batch_scorer.py -q</verify>
  <done>Los 4 tests de dispatch/score PASS; `test_if40_artifacts.py` y `test_batch_scorer.py` siguen PASS (retrocompat IF-40 y batch intactos). `_is_frame_v1` gobierna la selección de calculator+classifier por presencia de artefactos.</done>
</task>

<task type="auto">
  <name>Task 3: Propagar campos frame-v1 en el router /score</name>
  <files>/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scorer/routers/score.py, /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/tests/test_scorer_frame_v1_dispatch.py</files>
  <action>
En `scorer/routers/score.py::score_single` (líneas 42-51), extender la construcción de `ScoreResponse` para propagar los 3 campos desde `result`. Importar `FrameFlags` desde `scorer.schemas` (añadir al import de líneas 14-21). Ver 03-RESEARCH.md "Router cablea frame_flags":

```python
result = scorer.score(payment)

frame_flags_obj = None
if result.frame_flags is not None:
    frame_flags_obj = FrameFlags(**result.frame_flags)

return ScoreResponse(
    raw_score=result.score,
    percentile=result.percentile,
    risk_level=result.risk_level,
    is_anomaly=result.is_anomaly,
    factors=[FactorItem(**f) for f in result.factors],
    model_version=result.model_version,
    feature_version=result.feature_version,
    threshold_version=result.threshold_version,
    calibration_segment=result.calibration_segment,
    fallback_level=result.fallback_level,
    frame_flags=frame_flags_obj,
)
```

En modo IF-40, `result.calibration_segment`/`fallback_level` son `None` y `frame_flags_obj` es `None` → `ScoreResponse` los serializa como null (contrato ya los define Optional). Rails legacy no se ve afectado.

Añadir en `tests/test_scorer_frame_v1_dispatch.py`:
- `test_router_propagates_frame_v1_fields`: usar `fastapi.testclient.TestClient` con override de `get_scorer` (dependency) que retorne un scorer frame-v1 stub/real; POST a `/score` con un payment válido; afirmar que el JSON de respuesta tiene `calibration_segment`, `fallback_level` no-null y `frame_flags` dict con las 3 keys. Seguir el patrón de override de dependencias de FastAPI (`app.dependency_overrides[get_scorer] = ...`). Si el arranque completo de la app es costoso, alternativamente probar la lógica de construcción de `ScoreResponse` directamente con un `ScoringResult` frame-v1 mock (llamar la función o replicar el mapping) — el objetivo verificable es que los 3 campos llegan al `ScoreResponse`.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector && source venv/bin/activate && pytest tests/test_scorer_frame_v1_dispatch.py::test_router_propagates_frame_v1_fields -x -q && pytest tests/test_schemas_frame_v1.py -q</verify>
  <done>`/score` retorna `calibration_segment`, `fallback_level` y `frame_flags` poblados en modo frame-v1 y `null` en modo IF-40; `test_router_propagates_frame_v1_fields` PASS; `test_schemas_frame_v1.py` sigue PASS.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_scorer_frame_v1_dispatch.py -q` — toda la suite nueva PASS.
- `pytest tests/test_if40_artifacts.py tests/test_batch_scorer.py tests/test_artifact_loader.py tests/test_schemas_frame_v1.py -q` — retrocompat IF-40, batch y contrato intactos.
- Grep de sanidad: `grep -n "_is_frame_v1" src/fraud_detector/scoring/scorer.py` retorna la condición de dispatch por presencia (no por `len==30`).
- Confirmar que `_INSERT_COLUMNS` en `scorer/batch/scorer.py` NO fue tocado: `git diff --stat scorer/batch/scorer.py` vacío.
- Presupuesto de latencia: el path frame-v1 no añade I/O (stats en memoria); un smoke `scorer.score(payment, context=ctx)` debe retornar < 0.2s (verificar con `time.perf_counter` en un test si es barato).
</verification>

<success_criteria>
- El scorer despacha a frame-v1 sii `facility_stats` y `thresholds_segmented` están presentes; IF-40 legacy intacto (PLAT-01 reconciliado: resolución IANA autónoma, sin dependencia de Rails).
- `/score` propaga `calibration_segment`/`fallback_level`/`frame_flags`; `timezone_missing` observable para facilities desconocidas, sin excepción.
- `_INSERT_COLUMNS` del batch NO modificado (persistencia batch de nuevos campos diferida a Fase 4).
- Toda la suite de scorer PASS incluyendo tests de retrocompat.
</success_criteria>

<output>
After completion, create `/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/phases/03-wiring-del-scorer-e-integracion-platform/03-01-SUMMARY.md`.
</output>
