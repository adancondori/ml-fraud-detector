---
phase: 05-cola-hitl-y-captura-de-etiquetas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/packs/anomaly_detection/db/migrate/*_add_hitl_fields_to_anomaly_detection_triage_actions.rb
  - platform/db/schema.rb
  - platform/packs/anomaly_detection/app/models/anomaly_detection/triage_action.rb
  - platform/packs/anomaly_detection/app/forms/anomaly_detection/hitl_label_form.rb
  - platform/packs/anomaly_detection/app/services/anomaly_detection/hitl_label_service.rb
  - platform/packs/anomaly_detection/spec/forms/anomaly_detection/hitl_label_form_spec.rb
  - platform/packs/anomaly_detection/spec/services/anomaly_detection/hitl_label_service_spec.rb
autonomous: true

must_haves:
  truths:
    - "Un revisor puede registrar una etiqueta con los 6 campos de procedencia: reviewer_label, reviewed_at, reviewer_id (via performed_by_id), score_at_label_time, model_version_at_label, reviewer_saw_factors"
    - "hitl_queue_source ('top_k'|'below_p50') queda registrado en la TriageAction para trazar la procedencia de la cola"
    - "reviewer_label solo acepta el vocabulario alineado con la tesis: sospecha_fraude, anomalia_operativa, falso_positivo, indeterminado"
    - "reviewed_at se fija con Time.current en el service (no en el form, no Time.now)"
    - "La migracion es retrocompatible: todas las columnas nuevas son nullable / con default; el flujo de triage existente no se rompe"
  artifacts:
    - path: "platform/packs/anomaly_detection/db/migrate/*_add_hitl_fields_to_anomaly_detection_triage_actions.rb"
      provides: "Migracion que anade 6 columnas HITL-02 a anomaly_detection_triage_actions"
      contains: "ADD COLUMN"
    - path: "platform/packs/anomaly_detection/app/forms/anomaly_detection/hitl_label_form.rb"
      provides: "Form con validacion de reviewer_label (inclusion) y campos de procedencia"
      contains: "class HitlLabelForm"
    - path: "platform/packs/anomaly_detection/app/services/anomaly_detection/hitl_label_service.rb"
      provides: "Service que envuelve TriageTransitionService y enriquece la TriageAction con procedencia"
      contains: "class HitlLabelService"
  key_links:
    - from: "hitl_label_service.rb"
      to: "TriageTransitionService"
      via: "TriageTransitionService.call(form: triage_form) dentro de una transaccion"
      pattern: "TriageTransitionService"
    - from: "hitl_label_service.rb"
      to: "TriageAction (columnas HITL)"
      via: "triage_action.update!(reviewer_label:, reviewed_at: Time.current, ...)"
      pattern: "reviewer_label"
    - from: "hitl_label_form.rb"
      to: "VALID_REVIEWER_LABELS"
      via: "validates :reviewer_label, inclusion"
      pattern: "inclusion"
---

<objective>
Extender la superficie de triage existente para capturar etiquetas HITL con procedencia ML completa. Una migracion Rails retrocompatible anade 6 columnas a `anomaly_detection_triage_actions`; un `HitlLabelForm` valida el vocabulario de `reviewer_label`; un `HitlLabelService` envuelve el `TriageTransitionService` existente y enriquece la `TriageAction` resultante con la procedencia (score que vio el revisor, version del modelo, si vio los factores, y origen en la cola).

Purpose: Cierra HITL-02 (etiquetas con procedencia). Reusa Alert/TriageAction/TriageTransitionService en lugar de reinventar; extiende sin romper el flujo de triage vigente.
Output: Migracion + HitlLabelForm + HitlLabelService + specs, verificable dia-1 con FactoryBot.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cola-hitl-y-captura-de-etiquetas/05-RESEARCH.md

# Patrones exactos a seguir (auditados)
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/db/migrate/20260617040332_create_anomaly_detection_triage_actions.rb
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/models/anomaly_detection/triage_action.rb
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/forms/anomaly_detection/triage_transition_form.rb
@/Users/eidan/Documentation/Personal/Master/Perfil/platform/packs/anomaly_detection/app/services/anomaly_detection/triage_transition_service.rb
# Vocabulario de reviewer_label (alinear con tesis)
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scripts/hitl_ingest_labels.py
</context>

<constraints>
- TDD obligatorio (CLAUDE.md platform regla 8): failing test primero para form y service. Adversarial tests primero (regla, "Session Checklist"): etiqueta invalida, transaccion parcial, campo faltante.
- Sistema de pagos: NO commitear sin permiso explicito del usuario (regla 7). Presentar diff y esperar.
- Migracion (regla 13): NUNCA hand-pick el timestamp. Generar con `bundle exec rails g migration AddHitlFieldsToAnomalyDetectionTriageActions` (desde el root del platform) o `date -u +%Y%m%d%H%M%S`. NO usar tiempos redondos.
- Migracion retrocompatible: las 6 columnas nullable / con default. Usar el idioma del pack: `execute(<<-SQL.squish) ALTER TABLE ... ADD COLUMN` con guard `return unless table_exists?`. `down` con DROP COLUMN. Correr la migracion para actualizar `db/schema.rb`.
- reviewer_id NO se duplica (Pitfall 2): `performed_by_id` (FK users) YA es el reviewer_id. Exponer como `reviewer_id` en el form/API pero mapear a `performed_by`. NO anadir columna reviewer_id.
- 6 columnas nuevas: `reviewer_label VARCHAR(100) NULL`, `reviewed_at DATETIME(6) NULL`, `score_at_label_time DECIMAL(5,4) NULL`, `model_version_at_label VARCHAR(100) NULL`, `reviewer_saw_factors TINYINT(1) NOT NULL DEFAULT 0`, `hitl_queue_source VARCHAR(50) NULL`.
- `reviewed_at` con `Time.current` EN EL SERVICE (Pitfall 3 / regla timezone), nunca en el form ni Time.now.
- Vocabulario reviewer_label alineado con `VALID_CATEGORIES` de hitl_ingest_labels.py, EXCLUYENDO el sentinel `_correccion_`: `%w[sospecha_fraude anomalia_operativa falso_positivo indeterminado]`.
- El service corre dentro de `ActiveRecord::Base.transaction` (regla 3: operaciones financieras en transaccion); si el TriageTransitionService o el enrich falla, rollback total.
- Factories: usar `build`/`create` segun regla de velocidad; emails con SecureRandom si crea users; sin IDs hardcodeados.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Migracion + modelo — 6 columnas HITL en triage_actions</name>
  <files>platform/packs/anomaly_detection/db/migrate/*_add_hitl_fields_to_anomaly_detection_triage_actions.rb, platform/db/schema.rb, platform/packs/anomaly_detection/app/models/anomaly_detection/triage_action.rb</files>
  <action>
1. Generar el archivo de migracion con timestamp valido:
   `cd /Users/eidan/Documentation/Personal/Master/Perfil/platform && bundle exec rails g migration AddHitlFieldsToAnomalyDetectionTriageActions` (mueve el archivo generado al pack `packs/anomaly_detection/db/migrate/` si la convencion del pack lo requiere — verificar donde viven las migraciones del pack: `db/migrate/` del pack). Si el generador no apunta al pack, crear el archivo manualmente en `packs/anomaly_detection/db/migrate/` con nombre `<date -u +%Y%m%d%H%M%S>_add_hitl_fields_to_anomaly_detection_triage_actions.rb`.

2. Contenido de la migracion (idioma del pack, ver CreateAnomalyDetectionTriageActions):

```ruby
# frozen_string_literal: true

class AddHitlFieldsToAnomalyDetectionTriageActions < ActiveRecord::Migration[6.1]
  def up
    return unless table_exists?(:anomaly_detection_triage_actions)

    execute(<<-SQL.squish)
      ALTER TABLE anomaly_detection_triage_actions
        ADD COLUMN reviewer_label          VARCHAR(100) NULL,
        ADD COLUMN reviewed_at             DATETIME(6) NULL,
        ADD COLUMN score_at_label_time     DECIMAL(5,4) NULL,
        ADD COLUMN model_version_at_label  VARCHAR(100) NULL,
        ADD COLUMN reviewer_saw_factors    TINYINT(1) NOT NULL DEFAULT 0,
        ADD COLUMN hitl_queue_source       VARCHAR(50) NULL
    SQL
  end

  def down
    return unless table_exists?(:anomaly_detection_triage_actions)

    execute(<<-SQL.squish)
      ALTER TABLE anomaly_detection_triage_actions
        DROP COLUMN reviewer_label,
        DROP COLUMN reviewed_at,
        DROP COLUMN score_at_label_time,
        DROP COLUMN model_version_at_label,
        DROP COLUMN reviewer_saw_factors,
        DROP COLUMN hitl_queue_source
    SQL
  end
end
```

Nota: NO usar `ADD COLUMN IF NOT EXISTS` a menos que se confirme soporte en la version MySQL 8.0.42 del proyecto (research lo marca MEDIUM confidence). El guard `table_exists?` + migracion idempotente por schema_migrations es suficiente. Si se prefiere robustez, verificar con `bundle exec rails runner "puts ActiveRecord::Base.connection.select_value('SELECT VERSION()')"`.

3. Correr la migracion: `bundle exec rails db:migrate` (entorno dev/test). Verificar que `db/schema.rb` refleja las 6 columnas nuevas. Verificar rollback: `bundle exec rails db:rollback` no explota y luego `db:migrate` de nuevo.

4. Actualizar el modelo `TriageAction` para documentar `reviewer_id` como alias de `performed_by` (metodo de conveniencia) sin duplicar columna:
```ruby
# En anomaly_detection/triage_action.rb
alias_attribute :reviewer_id, :performed_by_id
```
(No anadir validaciones aqui — el vocabulario se valida en el form. Mantener el modelo delgado.)
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/platform && bundle exec rails db:migrate 2>&1 | tail -5 && bundle exec rails runner "cols = AnomalyDetection::TriageAction.column_names; puts %w[reviewer_label reviewed_at score_at_label_time model_version_at_label reviewer_saw_factors hitl_queue_source].all? { |c| cols.include?(c) }" — imprime true. Verificar db/schema.rb contiene las 6 columnas.</verify>
  <done>Las 6 columnas existen en `anomaly_detection_triage_actions`, todas nullable/con default. `db/schema.rb` actualizado. `TriageAction.reviewer_id` funciona via alias. Rollback reversible. El flujo de triage existente (TriageTransitionService) sigue funcionando (columnas opcionales).</done>
</task>

<task type="auto">
  <name>Task 2: HitlLabelForm (TDD) — validacion de vocabulario y campos de procedencia</name>
  <files>platform/packs/anomaly_detection/app/forms/anomaly_detection/hitl_label_form.rb, platform/packs/anomaly_detection/spec/forms/anomaly_detection/hitl_label_form_spec.rb</files>
  <action>
RED — Escribir `hitl_label_form_spec.rb` primero (patron ActiveModel::Model como TriageTransitionForm). El form hereda/compone los campos de triage (`alert`, `new_status`, `note`, `performed_by`) mas los HITL (`reviewer_label`, `reviewer_saw_factors`, `hitl_queue_source`). Validaciones:
- `reviewer_label` inclusion en `VALID_REVIEWER_LABELS = %w[sospecha_fraude anomalia_operativa falso_positivo indeterminado].freeze`.
- `hitl_queue_source` inclusion en `%w[top_k below_p50]` cuando presente (allow_nil).
- `alert`, `new_status`, `performed_by` presence (reusar la logica de transicion valida de TriageTransitionForm — puede heredar de el o componer VALID_TRANSITIONS).

Casos adversariales primero:
1. reviewer_label='fraude' (fuera del vocabulario) => invalido, error en :reviewer_label.
2. reviewer_label vacio => invalido.
3. hitl_queue_source='random' => invalido; ='top_k' => valido; =nil => valido.
4. reviewer_label='falso_positivo' + transicion valida + performed_by presente => valido.
5. reviewer_saw_factors acepta boolean (true/false) y se castea correctamente.
6. Sin performed_by => invalido.

VERIFICAR RED (falla por clase ausente).

GREEN — Implementar `HitlLabelForm`. Reusar `VALID_TRANSITIONS` (heredar de TriageTransitionForm o duplicar la constante con referencia). Exponer un metodo para construir el `TriageTransitionForm` interno (o exponer los atributos que el service necesita). Rubocop -A.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/platform && bundle exec rspec packs/anomaly_detection/spec/forms/anomaly_detection/hitl_label_form_spec.rb 2>&1 | tail -8 (verde) && bundle exec rubocop -A packs/anomaly_detection/app/forms/anomaly_detection/hitl_label_form.rb 2>&1 | tail -3</verify>
  <done>HitlLabelForm valida reviewer_label contra el vocabulario de 4 categorias, valida hitl_queue_source (allow_nil), y hereda las reglas de transicion/presence. Spec verde (>=6 ejemplos). Rubocop limpio.</done>
</task>

<task type="auto">
  <name>Task 3: HitlLabelService (TDD) — enriquecer TriageAction con procedencia</name>
  <files>platform/packs/anomaly_detection/app/services/anomaly_detection/hitl_label_service.rb, platform/packs/anomaly_detection/spec/services/anomaly_detection/hitl_label_service_spec.rb</files>
  <action>
RED — Escribir `hitl_label_service_spec.rb` primero (patron ApplicationService como TriageTransitionService). El service recibe `initialize(form:, score_row:)` donde `score_row` es el hash de la fila de anomaly_scores que vio el revisor (con "raw_score" y "model_version"). `.call`:
1. Construye el TriageTransitionForm interno desde el HitlLabelForm.
2. Dentro de `ActiveRecord::Base.transaction`: llama `TriageTransitionService.call(form: triage_form)`, obtiene la TriageAction de status_change creada, y la enriquece con `update!(reviewer_label:, reviewed_at: Time.current, score_at_label_time: score_row["raw_score"], model_version_at_label: score_row["model_version"], reviewer_saw_factors: form.reviewer_saw_factors ? 1 : 0, hitl_queue_source: form.hitl_queue_source)`.

Casos (usar FactoryBot: crear Alert con status 'reviewing', User como performed_by; `Timecop.freeze` para reviewed_at):
1. "persiste los 6 campos de procedencia en la TriageAction resultante": tras call, la ultima TriageAction status_change tiene reviewer_label, reviewed_at (== frozen time), score_at_label_time (== score_row raw_score), model_version_at_label, reviewer_saw_factors, hitl_queue_source correctos.
2. "reviewed_at usa Time.current (Timecop.freeze verifica el valor exacto)".
3. "reviewer_id queda como performed_by_id (no columna duplicada)".
4. "form invalido (reviewer_label fuera de vocabulario) NO crea TriageAction y NO cambia el status de la alerta" (adversarial: rollback / early return).
5. "si el enrich falla (p.ej. valor invalido) toda la transaccion hace rollback — la alerta no queda en el nuevo status" (adversarial: forzar fallo, expect no change).
6. "reviewer_saw_factors=false => persiste 0; =true => 1".

VERIFICAR RED.

GREEN — Implementar `HitlLabelService < ApplicationService` (ver Pattern 3 del research). Manejar el caso de form invalido (return sin crear). Time.current en el service. Rubocop -A. Correr la suite de services + forms del pack para regresion.
  </action>
  <verify>cd /Users/eidan/Documentation/Personal/Master/Perfil/platform && bundle exec rspec packs/anomaly_detection/spec/services/anomaly_detection/hitl_label_service_spec.rb packs/anomaly_detection/spec/forms/anomaly_detection/ 2>&1 | tail -10 (verde) && bundle exec rubocop packs/anomaly_detection/app/services/anomaly_detection/hitl_label_service.rb 2>&1 | tail -3</verify>
  <done>HitlLabelService persiste los 6 campos de procedencia en la TriageAction, reviewed_at con Time.current, reviewer_saw_factors como 0/1, hitl_queue_source registrado. Rollback total ante form invalido o fallo de enrich. Spec verde (>=6 ejemplos). Sin regresiones en forms/services del pack. Rubocop limpio.</done>
</task>

</tasks>

<verification>
- HITL-02: `HitlLabelService.call(form:, score_row:)` produce una TriageAction con los 6 campos: reviewer_label, reviewed_at, reviewer_id (=performed_by_id), score_at_label_time, model_version_at_label, reviewer_saw_factors — mas hitl_queue_source para trazar la procedencia de la cola.
- Retrocompatibilidad: la migracion es nullable/default; el TriageTransitionService existente y sus specs siguen verdes.
- reviewer_label restringido al vocabulario alineado con la tesis (4 categorias).
- Time.current (no Time.now), transaccion con rollback.
- Verificable dia-1: FactoryBot + Timecop, sin dependencia de datos shadow reales.
</verification>

<success_criteria>
- Migracion aplicada, 6 columnas presentes, schema.rb actualizado, rollback reversible.
- HitlLabelForm + HitlLabelService existen con specs verdes (>=12 ejemplos combinados).
- Suite de forms/services del pack sin regresiones. Rubocop limpio en archivos nuevos.
- Cambios presentados al usuario SIN commit (regla 7 platform).
</success_criteria>

<output>
After completion, create `.planning/phases/05-cola-hitl-y-captura-de-etiquetas/05-02-SUMMARY.md`.
Registrar: timestamp real de la migracion, nombres exactos de las 6 columnas y sus tipos, `VALID_REVIEWER_LABELS`, firma de `HitlLabelService.initialize(form:, score_row:)`, y confirmar que reviewer_id == performed_by_id (no duplicado).
</output>
