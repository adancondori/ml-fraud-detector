---
phase: 02-calibracion-segmentada-y-contrato-api
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - output/models/model_metadata_frame_v1.json
  - output/models/feature_list_frame_v1.json
  - scorer/artifact_loader.py
  - scorer/schemas.py
  - tests/test_artifact_loader.py
  - tests/test_schemas_frame_v1.py
autonomous: true

must_haves:
  truths:
    - "load_artifacts() carga facility_stats_v1.json y thresholds_segmented_v1.json en el dataclass Artifacts cuando el metadata los referencia; None cuando no (ruta legacy IF-40 intacta)"
    - "model_metadata_frame_v1.json tiene artifact_files, model_version, score_function, threshold_version, stats_artifact y thresholds_segmented_artifact"
    - "Una ScoreRequest sin facility_time_zone_iana valida OK con el campo en None (nunca default silencioso a 'UTC')"
    - "ScoreResponse acepta calibration_segment, fallback_level y frame_flags como campos estructurados"
    - "El path IF-40 legacy (model_metadata.json / final_feature_list.json) sigue cargando sin regresión (_validate_artifacts pasa sin cambios)"
  artifacts:
    - path: "scorer/artifact_loader.py"
      provides: "Artifacts con campos opcionales facility_stats y thresholds_segmented; load_artifacts los carga si el metadata los referencia"
      contains: "thresholds_segmented"
    - path: "scorer/schemas.py"
      provides: "ScoreRequest con currency/facility_time_zone_iana/amount_local opcionales; ScoreResponse con FrameFlags"
      contains: "class FrameFlags"
    - path: "output/models/model_metadata_frame_v1.json"
      provides: "Metadata frame-v1 con artifact_files y referencias a stats + thresholds segmentados"
      contains: "thresholds_segmented_artifact"
    - path: "output/models/feature_list_frame_v1.json"
      provides: "Lista de 30 nombres de features frame-v1 (análogo a final_feature_list.json)"
  key_links:
    - from: "scorer/artifact_loader.py"
      to: "output/models/thresholds_segmented_v1.json"
      via: "metadata.get('thresholds_segmented_artifact') -> json.loads(path)"
      pattern: "thresholds_segmented_artifact"
    - from: "scorer/artifact_loader.py"
      to: "output/models/facility_stats_v1.json"
      via: "metadata.get('stats_artifact') -> json.loads(path)"
      pattern: "stats_artifact"
    - from: "output/models/model_metadata_frame_v1.json"
      to: "output/models/feature_list_frame_v1.json"
      via: "artifact_files.feature_list apunta al nuevo JSON"
      pattern: "feature_list_frame_v1"
---

<objective>
Extender `Artifacts`/`load_artifacts` para cargar de forma retrocompatible `facility_stats_v1.json` y `thresholds_segmented_v1.json`, completar `model_metadata_frame_v1.json` con las keys faltantes (`artifact_files`, `model_version`, `score_function`, `threshold_version`, `thresholds_segmented_artifact`), crear `feature_list_frame_v1.json`, y bloquear el contrato Pydantic `frame-v1` (`ScoreRequest`/`ScoreResponse`) con campos opcionales sin defaults silenciosos y respuesta enriquecida.

Purpose: Sin `artifact_files` en el metadata, el loader no puede cargar el modelo frame-v1. Sin campos opcionales observables, la ausencia de `facility_time_zone_iana` se silenciaría con un default UTC (el sesgo exacto que el proyecto corrige). Cubre CAL-02, API-01, API-02.
Output: loader extendido, metadata completado, feature_list nuevo, schemas frame-v1.

CRÍTICO: Esta fase NO re-cablea el scorer en vivo (`SingleTransactionScorer` sigue en el path IF-40). Solo se tocan schemas y loader de forma retrocompatible. El router `scorer/routers/score.py` NO se modifica; con `currency: Optional[str] = None`, el scorer IF-40 existente hace `payment.get("currency","USD")` → `None or "USD"` = "USD" (backward compatible).
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calibracion-segmentada-y-contrato-api/02-RESEARCH.md
@scorer/artifact_loader.py
@scorer/schemas.py
@output/models/model_metadata_frame_v1.json

# Depende de que thresholds_segmented_v1.json exista (test de integración del loader)
@.planning/phases/02-calibracion-segmentada-y-contrato-api/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Completar model_metadata_frame_v1.json y crear feature_list_frame_v1.json</name>
  <files>output/models/model_metadata_frame_v1.json, output/models/feature_list_frame_v1.json</files>
  <action>
1. Crear `output/models/feature_list_frame_v1.json` con SOLO el array de los 30 nombres de features. Fuente canónica: el campo `feature_names` que ya existe dentro de `model_metadata_frame_v1.json` (o `FRAME_V1_FEATURE_NAMES` de `src/fraud_detector/scoring/features_frame_v1.py`). Análogo a `final_feature_list.json`. Generarlo programáticamente para evitar errores de transcripción:
   ```
   ./venv/bin/python -c "
   import json
   m=json.load(open('output/models/model_metadata_frame_v1.json'))
   names=m['feature_names']; assert len(names)==30, len(names)
   json.dump(names, open('output/models/feature_list_frame_v1.json','w'), indent=2)
   print('written', len(names))
   "
   ```

2. Añadir a `output/models/model_metadata_frame_v1.json` (preservando todas las keys existentes: `feature_version`, `feature_names`, `n_features`, `model_recipe`, `scaler_config`, `train_rows`, `built_at`, `stats_artifact`, `parity_check`, etc.) las keys faltantes al nivel raíz:
   ```json
   "model_version": "frame-v1",
   "score_function": "decision_function",
   "threshold_version": "segmented-v1",
   "artifact_files": {
     "model": "isolation_forest_frame_v1.joblib",
     "scaler": "scaler_frame_v1.joblib",
     "feature_list": "feature_list_frame_v1.json",
     "thresholds": "thresholds_segmented_v1.json"
   },
   "thresholds_segmented_artifact": "thresholds_segmented_v1.json"
   ```
   `stats_artifact: "facility_stats_v1.json"` ya existe — no duplicar. Editar el JSON preservando el resto (leerlo, actualizar el dict, reescribir con indent=2), no sobrescribir a ciegas.
  </action>
  <verify>
```
./venv/bin/python -c "
import json; m=json.load(open('output/models/model_metadata_frame_v1.json'))
assert m['artifact_files']['feature_list']=='feature_list_frame_v1.json'
assert m['artifact_files']['thresholds']=='thresholds_segmented_v1.json'
assert m['model_version']=='frame-v1' and m['score_function']=='decision_function'
assert m['thresholds_segmented_artifact']=='thresholds_segmented_v1.json'
fl=json.load(open('output/models/feature_list_frame_v1.json')); assert len(fl)==30
assert m['feature_names']==fl
print('OK')
"
```
  </verify>
  <done>`model_metadata_frame_v1.json` tiene artifact_files + model_version + score_function + threshold_version + thresholds_segmented_artifact sin perder keys previas; `feature_list_frame_v1.json` tiene los 30 nombres idénticos a feature_names.</done>
</task>

<task type="auto">
  <name>Task 2: Extender Artifacts y load_artifacts (retrocompatible)</name>
  <files>scorer/artifact_loader.py, tests/test_artifact_loader.py</files>
  <action>
En `scorer/artifact_loader.py`:

1. Añadir dos campos opcionales al dataclass `Artifacts` (frozen), DESPUÉS de los existentes para no romper llamadas posicionales:
   ```python
   facility_stats: dict | None = None
   thresholds_segmented: dict | None = None
   ```

2. En `load_artifacts()`, DESPUÉS de cargar `thresholds` y ANTES del `_validate_artifacts(...)`, añadir la carga condicional:
   ```python
   facility_stats = None
   stats_artifact = metadata.get("stats_artifact")
   if stats_artifact:
       stats_path = model_dir / stats_artifact
       if stats_path.exists():
           facility_stats = json.loads(stats_path.read_text())

   thresholds_segmented = None
   seg_artifact = metadata.get("thresholds_segmented_artifact")
   if seg_artifact:
       seg_path = model_dir / seg_artifact
       if seg_path.exists():
           thresholds_segmented = json.loads(seg_path.read_text())
   ```
   Pasar ambos al constructor de `Artifacts(...)`.

3. NO modificar `_validate_artifacts`: la validación existente `{'binary_threshold','score_percentiles'}` ya se satisface porque `thresholds_segmented_v1.json` (cuando se usa como `thresholds`) expone esas keys al nivel raíz. Para el metadata frame-v1, `artifact_files.thresholds = thresholds_segmented_v1.json`, así que `thresholds` cargado YA es el dict segmentado con las keys raíz — validación OK. Opcional: añadir un `warnings.warn(...)` si `facility_stats` está presente pero `thresholds_segmented` es None (inconsistencia de config), sin lanzar excepción.

Retrocompatibilidad: para el path IF-40 (metadata sin `stats_artifact` ni `thresholds_segmented_artifact`), ambos quedan `None` y `Artifacts` se comporta idéntico a antes.

4. Crear/extender `tests/test_artifact_loader.py`:
   - Test frame-v1: `load_artifacts(Path('output/models'))` con el metadata frame-v1 → `artifacts.facility_stats is not None`, `artifacts.thresholds_segmented is not None`, `'by_facility' in artifacts.thresholds_segmented`. (Nota: `output/models/model_metadata.json` puede existir y ganar prioridad en `_load_metadata`; si es así, el test debe cargar explícitamente desde un dir temporal que contenga solo los archivos frame-v1, o parametrizar. Verificar qué metadata resuelve `_load_metadata` en `output/models` y construir el test en consecuencia.)
   - Test legacy: un `Artifacts(...)` construido sin los campos nuevos → `facility_stats` y `thresholds_segmented` son None por default (backward compat de la firma).
  </action>
  <verify>`./venv/bin/python -m pytest tests/test_artifact_loader.py -q` pasa; `grep -n "thresholds_segmented\|facility_stats" scorer/artifact_loader.py` muestra los campos en el dataclass y la carga en load_artifacts.</verify>
  <done>Artifacts tiene facility_stats y thresholds_segmented opcionales (None=legacy); load_artifacts los carga si el metadata los referencia; _validate_artifacts sin cambios; tests frame-v1 y legacy en verde.</done>
</task>

<task type="auto">
  <name>Task 3: Contrato Pydantic frame-v1 (ScoreRequest/ScoreResponse)</name>
  <files>scorer/schemas.py, tests/test_schemas_frame_v1.py</files>
  <action>
En `scorer/schemas.py` (Pydantic v2, 2.12.5 — `Optional[str] = None` es opcional real, sin default silencioso):

1. En `ScoreRequest`:
   - Cambiar `currency: str = "USD"` (línea 30) por `currency: Optional[str] = None`. None = ausente; el scorer emitirá frame_flags o usará la currency del stats artifact (lógica del scorer es Fase 3).
   - Añadir `facility_time_zone_iana: Optional[str] = None`. NUNCA `= "UTC"` (Pitfall 9). None → frame_flags.timezone_missing=True.
   - Añadir `amount_local: Optional[float] = None` (solo logging/contexto, NO se pasa a calculate() — decisión del research open question 4).

2. Añadir clase `FrameFlags(BaseModel)`:
   ```python
   class FrameFlags(BaseModel):
       timezone_missing: bool = False
       currency_missing: bool = False
       currency_unknown: bool = False
   ```

3. En `ScoreResponse`, añadir DESPUÉS de los campos existentes (mantener orden y campos previos para backward compat de clientes Rails):
   ```python
   calibration_segment: Optional[str] = None
   fallback_level: Optional[str] = None
   frame_flags: Optional[FrameFlags] = None
   ```
   Estos son opcionales con `= None` para NO romper clientes legacy que no los consuman (distinto de ScoreRequest, donde `= None` significa "ausente → generar flag").

Crear `tests/test_schemas_frame_v1.py`:
- `ScoreRequest(user_id=1, facility_id=2, reservation_paid_out=10.0, created_at=datetime.now())` valida OK y deja `currency is None`, `facility_time_zone_iana is None`, `amount_local is None` (ninguno tiene default silencioso).
- Un `ScoreRequest` con `facility_time_zone_iana='America/La_Paz'` conserva el valor.
- `ScoreResponse(...)` sin los campos nuevos valida OK (backward compat) y `frame_flags is None`.
- `ScoreResponse(..., frame_flags=FrameFlags(timezone_missing=True), calibration_segment='currency:MYR', fallback_level='currency')` valida y round-trips por `model_dump()`.
- Afirmar explícitamente que `ScoreRequest` NO tiene `= "UTC"` para timezone: `ScoreRequest(...campos requeridos...).facility_time_zone_iana is None`.
  </action>
  <verify>`./venv/bin/python -m pytest tests/test_schemas_frame_v1.py -q` pasa; `grep -n "facility_time_zone_iana\|Optional\[str\] = None\|class FrameFlags\|amount_local" scorer/schemas.py` confirma los cambios y ausencia de `= "UTC"`.</verify>
  <done>ScoreRequest tiene currency/facility_time_zone_iana/amount_local opcionales sin default silencioso; FrameFlags definido; ScoreResponse enriquecido con calibration_segment/fallback_level/frame_flags opcionales; tests en verde.</done>
</task>

</tasks>

<verification>
- `model_metadata_frame_v1.json` completo (artifact_files + refs a stats y thresholds segmentados); `feature_list_frame_v1.json` con 30 nombres.
- `Artifacts`/`load_artifacts` cargan stats + thresholds segmentados; None→ruta legacy IF-40 sin regresión.
- `ScoreRequest` sin timezone valida con None (nunca 'UTC'); `ScoreResponse` con FrameFlags/calibration_segment/fallback_level.
- El scorer en vivo NO se re-cablea (router y SingleTransactionScorer intactos; grep confirma).
- `pytest tests/test_artifact_loader.py tests/test_schemas_frame_v1.py -q` en PASS.
</verification>

<success_criteria>
CAL-02, API-01 y API-02 satisfechos: el loader carga los artefactos de Fase 2 de forma retrocompatible (None→legacy); el contrato frame-v1 hace observable la ausencia de campos (timezone_missing) sin defaults silenciosos; la respuesta incluye calibration_segment, fallback_level y frame_flags. Ningún cambio al scoring en vivo salvo schemas/loader retrocompatibles.
</success_criteria>

<output>
After completion, create `.planning/phases/02-calibracion-segmentada-y-contrato-api/02-03-SUMMARY.md`
</output>
