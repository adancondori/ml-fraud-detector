---
phase: 02-calibracion-segmentada-y-contrato-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fraud_detector/stats/builder.py
  - scripts/build_facility_stats.py
  - tests/test_facility_stats_builder.py
  - output/models/facility_stats_v1.json
autonomous: true

must_haves:
  truths:
    - "facility_stats_v1.json contiene currency_fallbacks para las 14 monedas con train_n>=1000 (5 existentes + AUD, ILS, GTQ, PKR, HKD, AED, BWP, SGD, COP)"
    - "Las facilities fid=1214 (GTQ), fid=1232 (AUD), fid=1373 (HKD) pasan de fallback_level='global' a fallback_level='currency' tras regenerar"
    - "Las 580 facilities con fallback_level='facility' (n>=30) no cambian en absoluto"
    - "test_parity_phase1.py sigue en PASS (paridad batch<->real-time <1e-8) tras regenerar el artefacto"
  artifacts:
    - path: "src/fraud_detector/stats/builder.py"
      provides: "Criterio de currency_fallbacks por umbral de n en train (_MIN_CURRENCY_N=1000) en lugar de top-5"
      contains: "_MIN_CURRENCY_N"
    - path: "output/models/facility_stats_v1.json"
      provides: "Artefacto regenerado con currency_fallbacks ampliado"
      contains: "currency_fallbacks"
    - path: "tests/test_facility_stats_builder.py"
      provides: "Test del nuevo criterio de umbral de moneda"
      contains: "_MIN_CURRENCY_N"
  key_links:
    - from: "scripts/build_facility_stats.py"
      to: "src/fraud_detector/stats/builder.py"
      via: "FacilityStatsBuilder().build(train_df, tz_map, fid_currency)"
      pattern: "FacilityStatsBuilder\\(\\)\\.build"
    - from: "output/models/facility_stats_v1.json"
      to: "tests/test_parity_phase1.py"
      via: "el parity test carga el artefacto y compara batch vs real-time"
      pattern: "facility_stats_v1"
---

<objective>
Extender la generación de `currency_fallbacks` en `FacilityStatsBuilder` de un criterio top-5-por-volumen a un criterio por umbral de n en train (`_MIN_CURRENCY_N = 1000`), regenerar el artefacto `facility_stats_v1.json`, y confirmar que la paridad batch↔real-time de Fase 1 sigue intacta.

Purpose: La calibración segmentada de la Fase 2 comparte la estructura de moneda con el artefacto de stats. Sin fallbacks de moneda para AUD/ILS/GTQ/PKR/HKD/AED/BWP/SGD/COP, las facilities nuevas o de bajo volumen en esas monedas caen a `global` en lugar de a su moneda — perdiendo el marco de referencia correcto. Es prerequisito de la calibración (Plan 02-02).
Output: `facility_stats_v1.json` regenerado con 14 currency_fallbacks; builder y test actualizados.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calibracion-segmentada-y-contrato-api/02-RESEARCH.md
@.planning/phases/02-calibracion-segmentada-y-contrato-api/02-CONTEXT.md
@src/fraud_detector/stats/builder.py
@scripts/build_facility_stats.py
@tests/test_facility_stats_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reemplazar criterio de currency_fallbacks por umbral de n en train</name>
  <files>src/fraud_detector/stats/builder.py</files>
  <action>
En `src/fraud_detector/stats/builder.py`:

1. Reemplazar la constante de módulo `_N_CURRENCY_FALLBACKS = 5` (línea 26) por `_MIN_CURRENCY_N = 1_000  # monedas con menos rows en train caen a global`.

2. Dentro de `FacilityStatsBuilder.build()`, reemplazar el bloque de selección de `top_currencies` (líneas 66-76, el que usa `.value_counts().head(_N_CURRENCY_FALLBACKS)`) por selección basada en umbral, excluyendo explícitamente "EMPTY":
   ```python
   eligible_currencies = (
       train_df[train_df["currency"] != "EMPTY"]
       .groupby("currency")
       .size()
       .pipe(lambda s: s[s >= _MIN_CURRENCY_N])
       .index.tolist()
   )
   if "USD" not in eligible_currencies:
       eligible_currencies.append("USD")  # siempre incluir USD
   currency_fallbacks = self._compute_currency_fallbacks(train_df, eligible_currencies)
   ```
   Mantener la llamada existente a `self._compute_currency_fallbacks(...)` sin cambios en su firma.

3. Añadir la key `min_currency_n_threshold: int(_MIN_CURRENCY_N)` al dict de retorno (junto a `min_n_threshold` en la línea ~150) para que el artefacto sea autodescriptivo.

NO cambiar `MIN_N = 30` (guarda de facility, distinta de la de moneda). NO tocar `_compute_currency_fallbacks`, `_resolve_fallback`, ni el loop per-facility — solo cambia qué monedas entran a `currency_fallbacks`, no la aritmética de fallback de las facilities existentes.
  </action>
  <verify>
`./venv/bin/python -c "import ast; ast.parse(open('src/fraud_detector/stats/builder.py').read())"` sin error, y `grep -n "_MIN_CURRENCY_N\|_N_CURRENCY_FALLBACKS" src/fraud_detector/stats/builder.py` muestra `_MIN_CURRENCY_N` presente y `_N_CURRENCY_FALLBACKS` ausente.
  </verify>
  <done>`_N_CURRENCY_FALLBACKS` eliminado; `_MIN_CURRENCY_N=1000` gobierna la selección; el dict de retorno incluye `min_currency_n_threshold`; las funciones de fallback per-facility permanecen sin cambios.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerar facility_stats_v1.json y actualizar el test del builder</name>
  <files>scripts/build_facility_stats.py, tests/test_facility_stats_builder.py, output/models/facility_stats_v1.json</files>
  <action>
1. Ejecutar el script existente (NO crear uno nuevo — `scripts/build_facility_stats.py` ya materializa el artefacto desde `data/processed/train_features_enriched.parquet`):
   ```
   ./venv/bin/python scripts/build_facility_stats.py
   ```
   Esto sobrescribe `output/models/facility_stats_v1.json`. Confirmar en el output del script que `currency_fallbacks` ahora tiene 14 monedas y que `validate_universe_filter` pasa (assert de cobertura de las 1876 facilities). Si el script no imprime el conteo de currency_fallbacks, añadir una línea que lo imprima antes del `json.dump`.

2. Verificar el impacto declarado en el research con un chequeo puntual:
   ```
   ./venv/bin/python -c "
   import json; s=json.load(open('output/models/facility_stats_v1.json'))
   cf=set(s['currency_fallbacks']); print('n_currency_fallbacks:', len(cf)); print(sorted(cf))
   assert {'AUD','ILS','GTQ','PKR','HKD','AED','BWP','SGD','COP'} <= cf, 'faltan monedas mandatadas'
   for fid in ('1214','1232','1373'):
       print(fid, s['facilities'][fid]['fallback_level'])
       assert s['facilities'][fid]['fallback_level']=='currency', f'{fid} deberia ser currency'
   "
   ```

3. Actualizar `tests/test_facility_stats_builder.py`:
   - El test `test_builder_min_n` (línea ~134, `assert FacilityStatsBuilder.MIN_N == 30`) NO cambia.
   - `test_currency_fallbacks_usd_present` (línea ~200) se mantiene.
   - AÑADIR un test unitario nuevo `test_currency_fallback_threshold` que construya un `synthetic_train_df` con dos monedas: una con >=1000 rows y otra con <1000 rows, y afirme que solo la primera (más USD) aparece en `currency_fallbacks`. Importar `_MIN_CURRENCY_N` desde el módulo o replicar el umbral. Este test cubre el nuevo criterio a nivel unitario sintético (no depende del artefacto materializado).
   - AÑADIR un test de integración `test_materialized_artifact_has_mandated_currencies` marcado con `@pytest.mark.skipif(not Path('output/models/facility_stats_v1.json').exists(), reason='artefacto no materializado')` que cargue el JSON real y afirme `{'AUD','ILS','GTQ','PKR','SGD'} <= set(stats['currency_fallbacks'])`.
  </action>
  <verify>
`./venv/bin/python -m pytest tests/test_facility_stats_builder.py -q` pasa; el chequeo puntual del paso 2 imprime `n_currency_fallbacks: 14` y las 3 facilities en `currency`.
  </verify>
  <done>`facility_stats_v1.json` regenerado con 14 currency_fallbacks incluyendo las 9 monedas mandatadas; fid 1214/1232/1373 en fallback_level='currency'; nuevo test unitario del umbral + test de integración del artefacto pasan.</done>
</task>

<task type="auto">
  <name>Task 3: Re-correr paridad de Fase 1 sobre el artefacto regenerado</name>
  <files>output/models/facility_stats_v1.json</files>
  <action>
Ejecutar el guardrail de regresión de paridad batch↔real-time de Fase 1 contra el artefacto recién regenerado (NO modificar el test, solo correrlo):
```
./venv/bin/python -m pytest tests/test_parity_phase1.py -q
```
Este test carga `facility_stats_v1.json`, calcula features via `FrameV1FeatureCalculator.calculate()` (real-time) y `calculate_from_row()` (batch) sobre pagos del val set, y afirma diferencia máxima <1e-8.

Razón: ampliar `currency_fallbacks` solo cambia el fallback de facilities de bajo-n (cambia sus stats de magnitud de `global` a `currency`), lo que altera los vectores de features de esas facilities — pero afecta a batch y real-time por igual, así que la paridad debe mantenerse. Si el test FALLA por diferencia >1e-8, es un bug de asimetría batch/real-time introducido — reportar en el SUMMARY sin auto-corregir el artefacto (bloqueante para 02-02).

Registrar en el SUMMARY el `maxdiff` reportado y el número de pagos evaluados.
  </action>
  <verify>`./venv/bin/python -m pytest tests/test_parity_phase1.py -q` termina en PASS.</verify>
  <done>test_parity_phase1.py en PASS contra el artefacto regenerado; maxdiff <1e-8 registrado en el SUMMARY. Si falla, queda documentado como bloqueante para 02-02.</done>
</task>

</tasks>

<verification>
- `_MIN_CURRENCY_N=1000` reemplaza `_N_CURRENCY_FALLBACKS=5` en builder.py.
- `facility_stats_v1.json` tiene 14 currency_fallbacks (5 previas + AUD,ILS,GTQ,PKR,HKD,AED,BWP,SGD,COP).
- fid 1214/1232/1373 pasaron a fallback_level='currency'; las 580 facility-level intactas.
- `pytest tests/test_facility_stats_builder.py tests/test_parity_phase1.py -q` en PASS.
</verification>

<success_criteria>
El artefacto regenerado cubre todas las monedas con volumen de train suficiente vía cadena de fallback facility→currency→global, sin romper la paridad de Fase 1, dejando la base de moneda lista para la calibración segmentada de 02-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-calibracion-segmentada-y-contrato-api/02-01-SUMMARY.md`
</output>
