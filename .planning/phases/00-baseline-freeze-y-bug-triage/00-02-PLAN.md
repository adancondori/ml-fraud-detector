---
phase: 00-baseline-freeze-y-bug-triage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fraud_detector/data/loader.py
  - src/fraud_detector/features/engineering.py
  - scripts/run_fase7_evaluation.py
  - output/models/final_feature_list_operational.json
  - tests/test_currency_sanitize_phase0.py
autonomous: true

must_haves:
  truths:
    - "Una moneda 'EMPTY' o '' se sanea a 'USD' en la extracción, con warning logueado"
    - "El feature set operativo FS-frame-operational-v1 existe como artefacto versionado y NO incluye capture_delay_seconds"
    - "La ruta legacy thresholds.json (IF-31) ya no puede sobrescribirse con un threshold derivado del test set sin fallar/advertir; queda documentada como legacy"
    - "El threshold operativo activo (IF-40) usa thresholds_v2.json calibrado en val (threshold_source = percentile_95_validation_set)"
  artifacts:
    - path: "src/fraud_detector/data/loader.py"
      provides: "Sanitización de currency EMPTY/'' -> USD con warning en _postprocess_extraction"
      contains: "EMPTY"
    - path: "src/fraud_detector/features/engineering.py"
      provides: "Sanitización de currency EMPTY -> USD en el preprocesado de features"
      contains: "EMPTY"
    - path: "output/models/final_feature_list_operational.json"
      provides: "Lista cerrada FS-frame-operational-v1 (final_feature_list.json menos capture_delay_seconds)"
      contains: "FS-frame-operational-v1"
    - path: "scripts/run_fase7_evaluation.py"
      provides: "save_thresholds corregido a val set o marcado legacy no-op para IF-40"
      contains: "val"
    - path: "tests/test_currency_sanitize_phase0.py"
      provides: "Test que verifica EMPTY -> USD con warning"
      min_lines: 30
  key_links:
    - from: "src/fraud_detector/data/loader.py"
      to: "currency normalizer"
      via: "replace EMPTY/'' antes de normalize()"
      pattern: "replace.*EMPTY"
    - from: "output/models/final_feature_list_operational.json"
      to: "output/models/final_feature_list.json"
      via: "derivado excluyendo capture_delay_seconds"
      pattern: "capture_delay_seconds"
---

<objective>
Cerrar los tres bugs de saneamiento restantes que NO tocan el path de scoring real-time (por eso corren en paralelo con 00-01): (1) sanear la moneda `"EMPTY"` en la extracción como fix preventivo con reporte de calidad; (2) materializar la lista cerrada `FS-frame-operational-v1` excluyendo `capture_delay_seconds` (train/serve skew: valor real en batch, ~0 en real-time); (3) resolver la deuda del threshold legacy — documentar que el baseline usa IF-40 + `thresholds_v2.json` (ya calibrado en val) y evitar que la ruta legacy IF-31 sobrescriba `thresholds.json` con un percentil del test set.

Purpose: Estos tres saneamientos son independientes del fix de scoring (00-01) y no comparten archivos, por lo que ejecutan en la misma wave sin conflicto. Dejan el pipeline de datos y la configuración de features/thresholds en un estado limpio y documentado antes de congelar el baseline en 00-03.
Output: `loader.py` y `engineering.py` con replace de EMPTY; `final_feature_list_operational.json` versionado; `run_fase7_evaluation.py` corregido/marcado legacy; test de saneamiento de moneda verde.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/STATE.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/phases/00-baseline-freeze-y-bug-triage/00-RESEARCH.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/codebase/CONCERNS.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/data/loader.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/scripts/run_fase7_evaluation.py
</context>

<constraints>
- La moneda `"EMPTY"` NO aparece en los parquets actuales (0 filas en train/val verificado en research) — este es un fix PREVENTIVO para futuras extracciones (crítico para las facility stats de Fase 1). El reporte de calidad debe informar "0 filas afectadas en splits actuales" como confirmación positiva.
- El threshold IF-40 (`thresholds_v2.json`) YA está correcto (`percentile_95_validation_set`). NO re-generar ni tocar `thresholds_v2.json`. Solo la ruta legacy IF-31 (`thresholds.json` vía `run_fase7_evaluation.py`) tiene el bug del test set.
- `capture_delay_seconds` se EXCLUYE del feature set operativo por decisión de proyecto ya tomada (train/serve skew documentado en STATE.md); no re-litigar. No re-entrenar el modelo en esta fase — solo materializar la lista cerrada.
- No agregar dependencias nuevas.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Sanear moneda EMPTY/'' -> USD en loader.py y engineering.py con reporte de calidad</name>
  <files>src/fraud_detector/data/loader.py, src/fraud_detector/features/engineering.py</files>
  <action>
Añadir el replace de `"EMPTY"` (y `""`) a `"USD"` en los dos puntos donde la moneda se normaliza, con un warning + conteo de filas afectadas.

**En `src/fraud_detector/data/loader.py`, dentro de `_postprocess_extraction()` (bloque `if "currency" in out.columns:` línea 203), ANTES de llamar a `normalizer.normalize()`:**
```python
if "currency" in out.columns:
    _cur = out["currency"].fillna("USD").astype(str).str.upper()
    _empty_mask = _cur.isin(["EMPTY", ""])
    _n_empty = int(_empty_mask.sum())
    if _n_empty > 0:
        logger.warning(
            f"Sanitized {_n_empty} rows with currency EMPTY/'' -> USD"
        )
    out["currency"] = _cur.replace({"EMPTY": "USD", "": "USD"})
    normalizer = self._get_normalizer()
    out = normalizer.normalize(
        out,
        amount_col="amount",
        currency_col="currency",
        timestamp_col="created_at",
    )
```
Verificar que `logger` esté importado en loader.py (usar el import existente del proyecto: `from fraud_detector.utils.logger import logger`); si no está, añadirlo.

**En `src/fraud_detector/features/engineering.py` línea 646, REEMPLAZAR:**
```python
out["currency"] = out["currency"].fillna("USD").astype(str).str.upper()
```
POR:
```python
out["currency"] = (
    out["currency"].fillna("USD").astype(str).str.upper()
    .replace({"EMPTY": "USD", "": "USD"})
)
```

Este es un fix preventivo: los parquets actuales tienen 0 filas EMPTY, pero futuras extracciones (Fase 1 facility stats) pueden recibir EMPTY desde ClickHouse, donde `currency.utils.fallback_rate` lo trataría como rate=1.0 (USD) sin conversión — corrompiendo `log_amount`, `amount_usd_ratio`, `staff_amount_zscore`.
  </action>
  <verify>
`source venv/bin/activate && python -c "import pandas as pd; from fraud_detector.data.loader import DataManager" ` importa sin error. Test funcional inline: crear un DataFrame con currency=['EMPTY','','usd'] y verificar que tras el replace queda ['USD','USD','USD'].
  </verify>
  <done>
Ambos puntos (`loader.py:_postprocess_extraction` y `engineering.py:646`) sanean `"EMPTY"` y `""` a `"USD"`. `loader.py` loguea un warning con el conteo de filas afectadas. Import de `logger` presente.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test de saneamiento de moneda EMPTY</name>
  <files>tests/test_currency_sanitize_phase0.py</files>
  <action>
Crear un test que verifique el saneamiento en ambos puntos, siguiendo convenciones de TESTING.md (function-based OK aquí, synthetic DataFrame in-memory, no ClickHouse real).

```python
"""Fase 0: saneamiento de moneda EMPTY/'' -> USD (fix preventivo BASE-05)."""
from __future__ import annotations
import pandas as pd
import pytest

def test_engineering_currency_empty_sanitized():
    """engineering.py:646 debe mapear EMPTY/'' a USD."""
    # Construir el mínimo DataFrame que pase por el preprocesado de currency.
    # Verificar el resultado de la operación de replace directamente.
    s = pd.Series(["EMPTY", "", "usd", None]).fillna("USD").astype(str).str.upper()
    result = s.replace({"EMPTY": "USD", "": "USD"})
    assert list(result) == ["USD", "USD", "USD", "USD"]

def test_loader_currency_empty_sanitized_with_warning(caplog):
    """loader debe sanear EMPTY y loguear el conteo afectado."""
    # Si el saneamiento vive en un método aislable, invocarlo; si está inline en
    # _postprocess_extraction, construir un DataFrame mínimo y llamar al método
    # (mockeando normalizer.normalize como identidad si es necesario).
    ...
```

Elegir la estrategia según la estructura real de `loader.py`:
- Si el replace queda inline en `_postprocess_extraction`, testear vía un `DataManager` con `normalizer` mockeado (patrón MagicMock de TESTING.md) o extraer el replace a un helper estático `_sanitize_currency(series) -> series` y testear el helper directamente (preferido: más limpio y testeable). Si extraes el helper, usarlo también en engineering.py para DRY.
- Verificar que un input con >=1 EMPTY produce un warning capturable con `caplog` conteniendo el conteo.

Requerido: al menos 2 tests — uno para el mapeo de valores (EMPTY/''/None -> USD) y uno para el warning con conteo.
  </action>
  <verify>
`source venv/bin/activate && pytest tests/test_currency_sanitize_phase0.py -v` → todos verdes. `make test` completo sin regresiones.
  </verify>
  <done>
`tests/test_currency_sanitize_phase0.py` existe con >=2 tests verdes: mapeo EMPTY/''/None -> USD y warning con conteo de filas afectadas.
  </done>
</task>

<task type="auto">
  <name>Task 3: Materializar FS-frame-operational-v1 y resolver deuda del threshold legacy</name>
  <files>output/models/final_feature_list_operational.json, scripts/run_fase7_evaluation.py</files>
  <action>
**Parte A — Lista cerrada FS-frame-operational-v1:**
Generar `output/models/final_feature_list_operational.json` derivándolo de `output/models/final_feature_list.json` (lista de 40 features) excluyendo exactamente `capture_delay_seconds`. Escribir como objeto versionado (no solo la lista) para que sea un artefacto autodescriptivo:
```json
{
  "feature_set_id": "FS-frame-operational-v1",
  "derived_from": "final_feature_list.json",
  "excluded": ["capture_delay_seconds"],
  "exclusion_reason": "train/serve skew: valor real en batch, ~0 en real-time (AUC flag 0.511)",
  "n_features": 39,
  "features": [ ... 39 features en el mismo orden que final_feature_list.json, sin capture_delay_seconds ... ]
}
```
Generarlo con un script Python reproducible (no a mano): leer `final_feature_list.json`, filtrar `capture_delay_seconds`, escribir el JSON con `json.dumps(..., indent=2)`. Confirmar que `n_features == 39` y que `capture_delay_seconds` NO aparece en `features`.

**Parte B — Threshold legacy (BASE-03):**
El research confirmó que IF-40 usa `thresholds_v2.json` (val-calibrado, correcto) y la ruta legacy IF-31 usa `thresholds.json` (test-set, buggeado) generado por `scripts/run_fase7_evaluation.py:113-124` (`save_thresholds` con `threshold_source: "percentile_95_test_set"`).

Aplicar la opción pragmática del research (Opción B): NO re-generar `thresholds.json`. En `run_fase7_evaluation.py:save_thresholds`, evitar que la ruta legacy escriba un threshold del test set:
- Añadir en el JSON resultante un flag `"legacy_do_not_use_for_IF40": true` y cambiar `threshold_source` a `"percentile_95_test_set_LEGACY_DEPRECATED"`, O
- Preferido: parametrizar `save_thresholds(scores_if, split_name="val")` de modo que el default derive del val set y el `threshold_source` refleje el split real; y añadir un docstring/comentario en la cabecera de la función indicando que el scorer activo (IF-40) NO usa este artefacto sino `thresholds_v2.json`.

Elegir la variante que menos rompa `test_pipeline.py`/`test_evaluation.py`. Documentar la decisión en el SUMMARY. Si `save_thresholds` recibe scores del test set desde `run_pipeline.py`, la corrección de mínimo impacto es marcar el artefacto como legacy-deprecated + comentario; no reescribir el orquestador completo en esta fase.

NO tocar `output/models/thresholds_v2.json` bajo ninguna circunstancia.
  </action>
  <verify>
`source venv/bin/activate && python -c "import json; d=json.load(open('output/models/final_feature_list_operational.json')); assert d['n_features']==39; assert 'capture_delay_seconds' not in d['features']; assert d['feature_set_id']=='FS-frame-operational-v1'; print('OK 39 features, capture_delay excluida')"`. Para la Parte B: `grep -n 'threshold_source\|legacy\|thresholds_v2' scripts/run_fase7_evaluation.py` muestra el cambio; `make test` sin regresiones.
  </verify>
  <done>
`final_feature_list_operational.json` existe con `feature_set_id: FS-frame-operational-v1`, 39 features y sin `capture_delay_seconds`. `run_fase7_evaluation.py` ya no puede publicar silenciosamente un threshold del test set como operativo (marcado legacy-deprecated o derivado de val con flag); `thresholds_v2.json` intacto. Decisión documentada.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_currency_sanitize_phase0.py -v` verde.
- `final_feature_list_operational.json`: 39 features, sin `capture_delay_seconds`, `feature_set_id = FS-frame-operational-v1`.
- `thresholds_v2.json` sin modificar (`threshold_source` sigue siendo `percentile_95_validation_set`, `binary_threshold` = 0.024223975402714343).
- Ruta legacy `run_fase7_evaluation.py` no publica threshold del test set como operativo.
- `make test` completo sin regresiones.
</verification>

<success_criteria>
- BASE-05 satisfecho: EMPTY/'' saneado a USD en loader.py y engineering.py con warning + conteo; test verde.
- BASE-04 (parte exclusión) satisfecho: `FS-frame-operational-v1` materializado sin `capture_delay_seconds`.
- BASE-03 satisfecho: threshold operativo activo (IF-40) es val-calibrado; ruta legacy IF-31 corregida o marcada deprecated.
</success_criteria>

<output>
After completion, create `.planning/phases/00-baseline-freeze-y-bug-triage/00-02-SUMMARY.md`.
Documentar en el SUMMARY: (1) conteo de filas EMPTY en cada split actual (esperado 0 en train/val; verificar test), (2) la ruta absoluta de `final_feature_list_operational.json` y las 39 features, (3) la decisión tomada para la ruta legacy del threshold (deprecar vs corregir) y por qué. El plan 00-03 consume la lista operativa y el conteo de calidad de datos.
</output>
