---
phase: 00-baseline-freeze-y-bug-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fraud_detector/scoring/features.py
  - src/fraud_detector/scoring/features_enriched.py
  - tests/test_parity_phase0.py
autonomous: true

must_haves:
  truths:
    - "El scorer real-time carga las medias per-facility reales (no el global mean para todas las facilities)"
    - "El z-score de staff en real-time usa la stat (role, currency) correcta, no siempre el global"
    - "facility_avg_amount es idéntico (delta <1e-6) en batch y real-time para el mismo pago"
    - "staff_amount_zscore es idéntico (delta <1e-6) en batch y real-time para el mismo pago"
    - "Un artefacto corrupto o con nombre de atributo incorrecto falla ruidosamente (AssertionError), no silenciosamente con {}"
    - "Un captured_at malformado no lanza excepción; capture_delay devuelve 0.0"
  artifacts:
    - path: "src/fraud_detector/scoring/features.py"
      provides: "SingleFeatureCalculator con acceso directo a _facility_avg_amount y _role_currency_stats + lookup por clave (role, currency)"
      contains: "_facility_avg_amount"
    - path: "src/fraud_detector/scoring/features_enriched.py"
      provides: "_capture_delay_seconds con pd.isnull + try/except robusto"
      contains: "pd.isnull"
    - path: "tests/test_parity_phase0.py"
      provides: "Test de paridad batch↔real-time sobre >=500 pagos estratificados del val set"
      min_lines: 60
  key_links:
    - from: "src/fraud_detector/scoring/features.py"
      to: "fe._groups[4]._facility_avg_amount"
      via: "acceso directo de atributo + assert len>0"
      pattern: "_facility_avg_amount"
    - from: "src/fraud_detector/scoring/features.py"
      to: "fe._groups[6]._role_currency_stats"
      via: "lookup con clave tupla (role_key, currency)"
      pattern: "_role_currency_stats"
    - from: "tests/test_parity_phase0.py"
      to: "val_features_enriched.parquet"
      via: "lectura del set dorado como fuente de verdad batch"
      pattern: "val_features_enriched"
---

<objective>
Corregir el train/serve skew silencioso más grave del scorer: los dos `getattr` con nombres de atributo incorrectos en `SingleFeatureCalculator` que hacen que el scorer real-time SIEMPRE use el global mean para `facility_avg_amount` y global stats para `staff_amount_zscore`, mientras el modelo IF-40 fue entrenado con valores per-facility y per-(role,currency). Añadir un test de paridad batch↔real-time como guardrail de regresión permanente, y de paso robustecer el check `is pd.NaT` en `_capture_delay_seconds`.

Purpose: Este fix es el PREREQUISITO de toda la fase. Cambia los scores reales de producción, por lo que el baseline congelado (plan 00-03) debe medirse POST-fix. La paridad batch↔real-time es el concern de primera clase del proyecto — sin ella, cualquier métrica offline no es reproducible en vivo.
Output: `scoring/features.py` con acceso directo + assertions + lookup por tupla; `_capture_delay_seconds` robusto; `tests/test_parity_phase0.py` verde en CI.
</objective>

<execution_context>
@/Users/eidan/.claude/specops/workflows/execute-plan.md
@/Users/eidan/.claude/specops/templates/summary.md
</execution_context>

<context>
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/STATE.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/phases/00-baseline-freeze-y-bug-triage/00-RESEARCH.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/.planning/codebase/TESTING.md
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/features.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/features/engineering.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/src/fraud_detector/scoring/features_enriched.py
@/Users/eidan/Documentation/Personal/Master/Perfil/ml-fraud-detector/tests/test_scoring.py
</context>

<constraints>
- Restricción metodológica: proyecto no supervisado; el fix no debe tocar la lógica de entrenamiento (fit) — solo el path de inferencia real-time debe alinearse con lo aprendido en batch.
- El gate de éxito del proyecto es reducción de sesgo, no AUC. Este plan no mide AUC; solo corrige paridad.
- No agregar dependencias nuevas; el stack instalado (pandas 2.3.3, numpy, pytest) cubre todo.
- TDD: escribir el test de paridad de forma que FALLE con el código buggeado (si se ejecuta pre-fix) y PASE post-fix. Documentar el delta observado.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Corregir getattr + lookup de staff por clave (role, currency) en SingleFeatureCalculator</name>
  <files>src/fraud_detector/scoring/features.py</files>
  <action>
Corregir el bug doble de `getattr` con acceso directo + assertions post-carga, y reescribir el lookup de staff stats para usar la clave tupla real.

**En `__init__` (líneas 26-32), REEMPLAZAR:**
```python
self._global_avg_amount = fe._groups[0]._global_avg_amount
self._facility_avgs = getattr(fe._groups[4], "_facility_avg", {})
self._staff_stats = getattr(fe._groups[6], "_staff_stats", {})
logger.info(
    f"SingleFeatureCalculator loaded: global_avg={self._global_avg_amount:.2f}, "
    f"facilities={len(self._facility_avgs)}, staff_roles={len(self._staff_stats)}"
)
```
POR:
```python
self._global_avg_amount = fe._groups[0]._global_avg_amount
self._facility_avgs = fe._groups[4]._facility_avg_amount
self._staff_stats = fe._groups[6]._role_currency_stats
self._staff_currency_stats = fe._groups[6]._currency_stats
self._staff_global_mean = fe._groups[6]._global_mean
self._staff_global_std = fe._groups[6]._global_std
assert len(self._facility_avgs) > 0, (
    f"_facility_avg_amount vacío (grupo={type(fe._groups[4]).__name__}) — "
    "artefacto corrupto o nombre de atributo incorrecto"
)
assert len(self._staff_stats) > 0, (
    f"_role_currency_stats vacío (grupo={type(fe._groups[6]).__name__}) — "
    "artefacto corrupto o nombre de atributo incorrecto"
)
logger.info(
    f"SingleFeatureCalculator loaded: global_avg={self._global_avg_amount:.2f}, "
    f"facilities={len(self._facility_avgs)}, staff_role_currency={len(self._staff_stats)}"
)
```

**En `calculate()` (líneas 55-58), REEMPLAZAR:**
```python
is_staff = context.user_role in ("court_manager", "court_operator", "teacher")
role_key = context.user_role if is_staff else "player"
staff_mean = self._staff_stats.get(role_key, {}).get("mean", self._global_avg_amount)
staff_std = self._staff_stats.get(role_key, {}).get("std", 1.0)
```
POR (replica EXACTA de la cadena de fallback de `StaffRoleFeatures.transform()` en engineering.py:437-446):
```python
is_staff = context.user_role in ("court_manager", "court_operator", "teacher")
role_key = context.user_role if is_staff else "player"
currency = (payment.get("currency") or "USD").upper()
currency_key = (role_key, currency)
if currency_key in self._staff_stats:
    _s = self._staff_stats[currency_key]
elif currency in self._staff_currency_stats:
    _s = self._staff_currency_stats[currency]
else:
    _s = {"mean": self._staff_global_mean, "std": self._staff_global_std}
staff_mean = _s["mean"]
staff_std = _s["std"] or 1.0
```

**CRÍTICO:** el bug no es solo el nombre del atributo. `_role_currency_stats` indexa por tuplas `(str, str)` = `(role, currency)`, no por `str(role)`. Cambiar solo el nombre del atributo dejaría el `.get(role_key, {})` devolviendo `{}` para todas las consultas porque ninguna clave es un string puro. Por eso hay que reescribir el lookup completo. El orden de fallback (role+currency → currency → global) debe coincidir con engineering.py:439-444 para garantizar paridad.

NO tocar la construcción del array `features` (líneas 81-121) ni el resto del archivo. `facility_avg` en línea 54 (`self._facility_avgs.get(fid, self._global_avg_amount)`) ya es correcto una vez que `_facility_avgs` apunta al dict real.
  </action>
  <verify>
`source venv/bin/activate && python -c "from fraud_detector.scoring.features import SingleFeatureCalculator; c=SingleFeatureCalculator('output/models/feature_engineer.joblib'); print('facilities', len(c._facility_avgs)); print('staff_keys', list(c._staff_stats.keys())[:3]); assert len(c._facility_avgs) > 0 and len(c._staff_stats) > 0"` imprime facilities>0 y muestra claves tupla `(role, currency)`.
  </verify>
  <done>
`SingleFeatureCalculator` carga `_facility_avg_amount` y `_role_currency_stats` reales (dicts no vacíos), las assertions no disparan con el artefacto de producción, y el lookup en `calculate()` usa la clave tupla `(role_key, currency)` con la cadena de fallback role+currency → currency → global idéntica a `StaffRoleFeatures.transform()`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Robustecer _capture_delay_seconds contra NaT y strings malformados</name>
  <files>src/fraud_detector/scoring/features_enriched.py</files>
  <action>
Corregir el check `is pd.NaT` (identidad, no funciona para Timestamps construidos desde ciertos strings) y proteger contra strings que revientan al parsear.

**En `_capture_delay_seconds` (líneas 104-116), REEMPLAZAR el cuerpo desde línea 110:**
```python
        captured = pd.Timestamp(captured_at)
        created = pd.Timestamp(created_at)
        if captured is pd.NaT or created is pd.NaT:
            return 0.0

        delay = (captured.to_pydatetime() - created.to_pydatetime()).total_seconds()
        return float(np.clip(delay, -86400, 86400))
```
POR:
```python
        try:
            captured = pd.Timestamp(captured_at)
            created = pd.Timestamp(created_at)
        except (ValueError, TypeError):
            return 0.0
        if pd.isnull(captured) or pd.isnull(created):
            return 0.0

        delay = (captured.to_pydatetime() - created.to_pydatetime()).total_seconds()
        return float(np.clip(delay, -86400, 86400))
```

Mantener intacto el guard previo (líneas 107-108: `if not captured_at or not created_at: return 0.0`). El fix usa `pd.isnull()` (cubre todos los paths de construcción de NaT) y envuelve el parseo en try/except para que un string como `"invalid-date"` devuelva 0.0 en vez de lanzar `ValueError`. No cambiar la firma ni el clip a [-86400, 86400].
  </action>
  <verify>
`source venv/bin/activate && python -c "from fraud_detector.scoring.features_enriched import *" ` importa sin error, y un test inline: `python -c "import pandas as pd, numpy as np; from fraud_detector.scoring.features_enriched import EnrichedFeatureCalculator as E" ` — o el equivalente si el método es estático — verificar que `_capture_delay_seconds({'captured_at':'invalid-date','created_at':'2025-01-01'})` retorna `0.0` sin excepción.
  </verify>
  <done>
`_capture_delay_seconds` usa `pd.isnull()` en vez de `is pd.NaT`, envuelve el parseo en try/except (ValueError, TypeError), y devuelve 0.0 para captured_at malformado, None o NaT — sin lanzar excepción nunca.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test de paridad batch↔real-time sobre >=500 pagos estratificados del val set</name>
  <files>tests/test_parity_phase0.py</files>
  <action>
Crear el guardrail de regresión central de la fase. El test toma filas reales de `data/processed/val_features_enriched.parquet` (batch = fuente de verdad, ya calculado por FeatureEngineer) y verifica que `SingleFeatureCalculator.calculate()` produce valores idénticos para `facility_avg_amount` (índice 19 de FEATURE_NAMES) y `staff_amount_zscore` (índice 27).

Estructura del test (seguir convenciones de tests/test_scoring.py y TESTING.md — class-based, fixtures module-scoped, np.testing):

```python
"""Fase 0: paridad batch <-> real-time del SingleFeatureCalculator."""
from __future__ import annotations
from pathlib import Path
import numpy as np
import pandas as pd
import pytest
from fraud_detector.features.engineering import FEATURE_NAMES
from fraud_detector.scoring.features import SingleFeatureCalculator
from fraud_detector.scoring.context import UserContext

FE_PATH = "output/models/feature_engineer.joblib"
GOLDEN_PARQUET = "data/processed/val_features_enriched.parquet"
FACILITY_AVG_IDX = FEATURE_NAMES.index("facility_avg_amount")   # 19
STAFF_ZSCORE_IDX = FEATURE_NAMES.index("staff_amount_zscore")   # 27
TOL = 1e-6
N_ROWS = 500
MIN_FACILITIES = 20

pytestmark = pytest.mark.skipif(
    not Path(FE_PATH).exists() or not Path(GOLDEN_PARQUET).exists(),
    reason="requiere artefactos entrenados y val_features_enriched.parquet",
)

@pytest.fixture(scope="module")
def golden_rows():
    df = pd.read_parquet(GOLDEN_PARQUET)
    # Estratificar por facility_id: >=20 facilities, >=500 filas totales.
    sampled = (
        df.groupby("facility_id", group_keys=False)
        .apply(lambda g: g.head(max(1, N_ROWS // MIN_FACILITIES)))
    )
    if len(sampled) < N_ROWS:
        sampled = df.head(N_ROWS)
    sampled = sampled.head(max(N_ROWS, len(sampled))).reset_index(drop=True)
    assert sampled["facility_id"].nunique() >= MIN_FACILITIES
    assert len(sampled) >= N_ROWS
    return sampled

@pytest.fixture(scope="module")
def calculator():
    return SingleFeatureCalculator(FE_PATH)

def _row_to_payment(row) -> dict:
    return {
        "reservation_paid_out": float(row.get("amount", 0) or 0),
        "created_at": row["created_at"],
        "facility_id": int(row["facility_id"]),
        "currency": str(row.get("currency", "USD") or "USD"),
        "discount": 0.0, "tip": 0.0,
    }

def _row_to_context(row) -> UserContext:
    return UserContext(user_role=str(row.get("user_role", "player") or "player"))
```

Tests requeridos (class `TestParityPhase0`):
1. `test_facility_avg_parity(golden_rows, calculator)`: para cada fila, compara `row["facility_avg_amount"]` (batch) contra `calculator.calculate(payment, context)[FACILITY_AVG_IDX]` (real-time). Asserta `max(abs(delta)) < TOL` sobre las N filas. Reporta el delta máximo en el mensaje de assert.
2. `test_staff_zscore_parity(golden_rows, calculator)`: mismo patrón contra `row["staff_amount_zscore"]` e índice `STAFF_ZSCORE_IDX`. Nota: `staff_amount_zscore` depende de `amount`, `user_role` y `currency`; el payment/context construidos deben pasar los tres. Tolerancia <1e-6 (mismo cómputo determinístico).
3. `test_facility_avgs_not_global_fallback(calculator)`: assert que `len(calculator._facility_avgs) > 1` y que NO todas las facilities mapean al mismo valor global — prueba de regresión directa del bug getattr (con el bug, `_facility_avgs` sería `{}` y todo caería al global).

Si al construir payment/context faltan campos que `calculate()` necesita (revisar la firma real), completar con neutros (0.0 / listas vacías) — solo `facility_avg_amount` y `staff_amount_zscore` son load-bearing para la paridad. Documentar en un comentario que las demás features no se verifican en este test (velocidad/comportamiento dependen de context rolling que el parquet enriquecido no reconstruye 1:1).
  </action>
  <verify>
`source venv/bin/activate && pytest tests/test_parity_phase0.py -v` → los 3 tests PASAN. Adicionalmente `make test` no rompe tests existentes (`test_scoring.py::test_feature_count_is_31`, `test_features_are_finite`, `test_scorer_returns_scoring_result` siguen verdes).
  </verify>
  <done>
`tests/test_parity_phase0.py` existe con >=3 tests que pasan: paridad de `facility_avg_amount` (<1e-6), paridad de `staff_amount_zscore` (<1e-6), y regresión anti-getattr (facility_avgs no colapsa al global). El set estratificado cubre >=20 facilities y >=500 filas. `make test` completo pasa sin regresiones.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_parity_phase0.py -v` verde (3/3).
- `make test` completo sin regresiones (los tests de test_scoring.py y test_if40_artifacts.py que no validan valores específicos de facility_avg/staff_zscore siguen pasando; documentar cualquier test que cambie de comportamiento).
- Import limpio de `SingleFeatureCalculator` con assertions activas sobre el artefacto de producción.
- Ejecutar el scorer sobre una muestra de val ANTES y DESPUÉS del fix (opcional pero recomendado por research pitfall #2) y anotar el delta de scores para consumir en el plan 00-03.
</verification>

<success_criteria>
- BASE-01 satisfecho: getattr corregidos con acceso directo + assertions; lookup de staff por clave (role, currency) con fallback correcto.
- BASE-02 satisfecho: test de paridad batch↔real-time verde como guardrail permanente.
- BASE-04 (parte NaT) satisfecho: `is pd.NaT` reemplazado por `pd.isnull()` + try/except.
- Ningún test existente se rompe por el cambio.
</success_criteria>

<output>
After completion, create `.planning/phases/00-baseline-freeze-y-bug-triage/00-01-SUMMARY.md`.
Documentar en el SUMMARY: (1) el delta de scores observado pre/post fix sobre la muestra de val (evidencia de que el bug estaba activo), (2) qué tests existentes cambiaron de comportamiento (si alguno), (3) el delta máximo de paridad medido (debe ser <1e-6). Estos datos los consume el plan 00-03 para el baseline congelado.
</output>
